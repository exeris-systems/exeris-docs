---
title: "Documentation sweep 2026-09 — exeris-docs"
type: working-note
visibility: public
owning-repo: exeris-docs
status: active
last-verified: 2026-09-04
---

# Documentation sweep 2026-09 — exeris-docs

The ADR-085 sweep of this repository. Records live at the repo root, so `adr/`, `rfc/`,
`templates/` and the root `*.md` files are the documentation tree; `standards/` and every
`_`-prefixed directory were out of scope by instruction.

- **Source branch:** `docs/sweep-2026-09`, branched from `main` (`6b3f6b7`). The local
  `docs/reserve-083-084` branch was not used — its three commits are already in `main` as the
  squashed `#86`, and `main` additionally carries `#87`, which that branch predates.
- **Validators:** the shared org-github bundle (md5 `684d31a67bcf4d00d66d35bca9f075de`), extracted
  once to `~/exeris-systems/.guardrails/orggh/` and reached from each repo through a git-excluded
  `.guardrails` symlink. Vale 3.20.0 was installed for this sweep; it was absent. The bundle itself
  and the rest of the Phase 0 material live under `standards/_*/`, which is local-only working
  material and is not committed.
- **This sweep's cross-repo section** is written to `standards/_inventory/sweep-2026-09-report.md`,
  which stays local for the same reason. It is the aggregate across repositories; this file is the
  record that ships.

## Before and after

| Check | Before | After |
|:--|--:|--:|
| `frontmatter_check.py --mode strict` on the CI scope (`adr rfc *.md`) | 20 errors | **0 errors**, 1 warning |
| the same, with `--section-check` enabled | not run | **0 errors** |
| the same, with `templates/*.md` added to the scope | 20 errors | 3 errors — one known false positive, three times |
| `registry_check.py --siblings-root ..` (registry mode) | 2 errors | **0 errors** |
| `claude_md_check.py` | 9 errors | **0 errors**, 1 warning |
| Vale, error level | 2 errors | **0 errors** |
| Vale, all levels | 1374 warnings / 985 suggestions | 1454 / 1044 |

Vale's warning and suggestion counts rose because the sweep added text — frontmatter blocks, the
`## Rule levels` / `## Scoped bans` / `## Cross-repo ADRs to consult` / `## Conventions` sections
required by `claude-md-schema.md`, and 58 VERIFY markers. No warning-level rule regressed on
text that existed before the sweep.

## The two Vale errors were withdrawal text, not live claims

This is the finding that most changes what the sweep did. `b2b-technical-whitepaper.md:10` and
`high-level-architecture.md:173` matched `Exeris.RetractedFigures`, but both matches sit inside the
sentence that **withdraws** the figure:

> earlier revisions of this document asserted "up to 60% of CPU cycles" and a ">160 GB allocation on
> a 4 GB payload" figure that no campaign in `exeris-benchmarks` supports. Both are withdrawn.

Under rule 4 that text is preserved verbatim and fenced with the sanctioned inline toggle, which is
what was done. In the HLA the withdrawal lives inside a table cell, so the toggle comments were
placed inline within the cell rather than on their own lines; the table still renders. The elided
report path in the same cell was expanded to the full, checkable
`results/reports/2026-07-21-entity-read-by-id-tuned-pg-triad-comparison-eligible.md`, verified to
exist on `exeris-benchmarks` `origin/main`.

**`exeris-benchmarks/docs/CLAIMS.md` is not in that repository's working tree.** The checkout is on
`chore/community-app-kernel-0.7.1`, which predates the file; it exists only on `origin/main`. Four
standards files, this repo's `CLAUDE.md` and the whitepaper all cite it by that path. Every rule-4
judgement in this sweep was made against a verbatim 989-line copy taken from `origin/main`.

## Triage

| Path | Bucket | Reason | Where history went |
|:--|:--|:--|:--|
| adr/ADR-020-open-core-documentation-mirror-policy.md | QUESTION | Frontmatter added and three mechanical fixes applied (one dangling demonstrative, two British-spelling verbs); all four substantive findings are founder-reserved — a live contradiction over `exeris-telemetry-spec`'s visibility, two stale re … | - (nothing removed; the "Discovered during the 2026-05-07 cleanup" passages at lines 23, 42-44 and 46 are ADR *Context* — the problem statement that drove the decision, historical by nature — and the line-88 relocation claim is verified pro … |
| adr/ADR-053-sku-composition-manifest-format.md | QUESTION | Frontmatter added and one VERIFY marker inserted; the body carries a confirmed-false Jackson-version claim in an accepted record that rules 5 and 8 reserve to the founder, so no prose was edited. | - (no historical/archaeology prose in the file; rule-3 grep for "previously / used to / no longer / historically / originally / after the refactor" returns nothing, and there is no retraction box or CLAIMS.md-governed figure anywhere on the … |
| adr/ADR-001-cloud-native-and-agnostic-infrastructure-strategy.md | DEAD-REF (fixed) | Two dead references repaired: `CitadelRepository` no longer exists in live kernel source (SPI/Core/Community) and the Compliance link pointed at a file carrying none of the pillar material; frontmatter added, decision argument untouched. | - (no history removed; the dead symbol survives in Corelio/ and exeris-kernel-enterprise/kernel-legacy/, and its stale doc copy at exeris-kernel/docs/modules/01-spi.md:53 is a `[DOC DEBT]` item for that repo) |
| adr/ADR-002-unified-database-strategy.md | QUESTION | "(replacing Neo4j)" in Decision text needs a founder ruling (Amendments entry + registry status) — not edited; the only edit is the `unartifacted` figure state on the >50k writes/sec figure in Context, plus frontmatter and one VERIFY marker … | - (nothing removed; the Neo4j question goes to questions[], the figure question travels in the VERIFY marker on line 29) |
| adr/ADR-004-jdk-26-ea-preview-features-mandate.md | QUESTION | Every remaining defect is record text under rule 5 (Decision, five obligations, one Context premise, two Consequences bullets, Engineering Protocol); in the category I was authorised to fix — dead references, expired "currently / for now / … | Nothing deleted. The 2025-12-26 decision text and the 2025 History note stay verbatim; the drift goes to a founder-authored `## Amendments` section at the end of the file (adr-conventions rule 7), with adr-index.md:25 moving in the same cha … |
| adr/ADR-006-spring-free-kernel-boundary.md | QUESTION | ACCEPTED record: 7 confirmed defects (boot DAG, TelemetrySink, guard-test attribution, servlet bridge, ~30 MB, < 5 µs PAQS, missing cross-repo stubs) all resolve to founder-gated Amendments entries or registry edits under rules 5 and 8, so … | Pre-0.11 DAG shape stays in the ADR text pending an Amendments entry that preserves it; live DAG stays canonical in exeris-kernel/docs/subsystems/bootstrap.md (documentation-precedence rule 1) and must not be duplicated here. |
| templates/ADR-TEMPLATE.md | KEEP | Frontmatter already correct in the working tree (lines 1-9, slug present); no confirmed rewrite; the three proposed in-body VERIFY markers were refuted as template-propagating. Not touched. | - |
| templates/RFC-TEMPLATE.md | REWRITE | Usage note 4 denied the existence of a RESEARCH document type that six binding sources and a live gate regex all define; replaced with the routing rule. Frontmatter already present. | - |
| templates/RESEARCH-TEMPLATE.md | REWRITE | Only hard build error of the four (missing frontmatter) — block inserted; Hypothesis prose narrowed the template to "Exeris Kernel" against its own README and three live non-kernel consumers, and used first person against docs-style-guide r … | - |
| templates/README.md | REWRITE | Filenames list stated an ungated Research form and omitted the `.link.md` stub form; "frontmatter" ambiguously named the ADR header table now that a real YAML block exists above it. Frontmatter-exempt, so prose only. | - |
| rfc/RFC-2026-06-25-publishable-unit-marketplace.md | QUESTION | Frontmatter + one broken HLA section pointer (§3.2 → §3.3, wrong on the day it was written, so no history lost); the five real drifts — ADR-024 composition-runtime ownership, the caps repo now existing, the sibling RFC being ACCEPTED, and t … | Nothing deleted. The four present-state drifts belong in a dated editor's note under a new `## Amendments`-style heading, or the RFC stays a 2026-06-25 snapshot superseded when it closes — founder's choice |
| rfc/RFC-2026-09-04-docs-guardrails-open-decisions.md | QUESTION | Frontmatter + one VERIFY marker for the missing `docs-guardrails-plan.md`; body untouched — two wrong figures (`5` vs 11 schema `$id`; 'every published pom.xml'), the unreserved `ADR-DOCS` number, and the open Decision Record are all founde … | Nothing deleted; no history in the file — it is a 2026-09-04 draft, not a record of past state |
| adr/ADR-023-capability-licensing-taxonomy.md | REWRITE | Registry landed but CI job did not; prescriptive trigger fired 2026-08-16 and cited a whitepaper row ("Q1 2027") that does not exist; source-of-truth line still gated on "until the registry lands". | Nothing relocated — the founding 2026-05-12 inventory and its 2026-08-16 growth note were already history-safe in the file's own blockquote; the 2026-05-13 same-day amendment untouched. |
| adr/ADR-024-capability-composition-model.md | REWRITE | Body still named the kernel as the refusing party, cited a non-existent `KERNEL_TRANSPORT` identifier and a non-existent ADR-020 manifest clause; all five prescriptive triggers and three of five follow-ups have fired since. | The "greenfield in `exeris-platform` as of 2026-06-17" fact stays recorded in the 2026-06-17 amendment (line 152 post-edit) and the 2026-06-25 amendment's opening paragraph — both untouched. |
| adr/ADR-085-documentation-architecture-and-repo-hygiene-standards.md | QUESTION | Untracked, PROPOSED record; frontmatter added and two VERIFY markers inserted. All thirteen skeptic-confirmed corrections are rule-8 founder decisions (nine ungated figure/scope fixes, three Decision-text obligations, one Cross-references t … | - (nothing deleted; no history construction in the file — a scan for previously/used to/no longer/historically/originally/after the refactor/we fixed/the bug was returns nothing, and the one 'as of 2026-09-04' is a record-header date) |
| adr-index.md | QUESTION | Three authorised edits applied and both validators now clean (registry_check: 94 files, 0 errors; frontmatter_check: 0 errors); the residue is eight founder rulings the sweep may not make under rule 8 — six confirmed-false or stale registry … | Nothing deleted. Row 040/049/050 provenance kept intact (only the strike was withheld); rows 064/066 pre-decision prose left in place with a VERIFY marker naming ADR-064's `## Context and Problem Statement`, `exeris-kernel/docs/subsystems/f … |
| b2b-technical-whitepaper.md | QUESTION (skeptic re-bucket; 18 mechanical rewrites applied under the skeptic's explicit "safe to land now" carve-out) | Frontmatter added; the line-10 withdrawal wrapped in the sanctioned Vale toggle unchanged; an unartifacted saga coordinator figure that CLAIMS.md's citation canon forbids removed from §4.1; `@RequiresRole` re-attributed from exeris-sdk to e … | Nothing deleted silently. The 2026-05-05 saga figures were never in any artefact, so there is no measurement to preserve; the structural claim they were attached to already stands in §4.1 rows 219/224 and ADR-013 §3. |
| cap-author-guide.md | REWRITE | Frontmatter added (`type: howto`); both L1-required sections (`## Prerequisites`, `## Where this does not apply`) added from verified source; three archaeology passages rewritten as present state; fabricated `ObservabilityBridge` `@Requires … | Deadlock/`GenerateMojo` history already lives in `GenerateMojo.java:43-51`; the unversioned-provide bug in `CompositionBinding.java:16-22` and ADR-024's obligation-8b altitude table; the tooling-0.7.0 SNAPSHOT episode in this file's own com … |
| cap-license-registry.md | REWRITE | Frontmatter added (`type: reference`); dead "Owns ADR-023 open follow-up 1" pointer replaced with "Discharges" (the follow-up is struck through in ADR-023:171); self-dating "as of this writing" hedge replaced by the named cap; one blank lin … | - |
| CLAUDE.md | REWRITE | Frontmatter added; 12 confirmed sentence-level corrections applied (dead execution-plan and 2026-05-13 references removed, bootstrap DAG re-stated from SubsystemOrchestrator.java, PG "v3" dropped, Tier 2 cap count grounded in cap-license-re … | Bootstrap fork removal: already recorded in exeris-kernel's ADR-066 and bootstrap.md:340-355 — no exeris-docs page needs it. The v0.9-label drop: commit 732e468 and whitepaper §7's own row. |
| README.md | REWRITE | No frontmatter (rule-5 exemption, verified against frontmatter_check.py:35 and empirically). Six factual fixes: dead execution-plan reference, Context-Centric CRM removed from the SKU list per ADR-023:115, three ADRs added to the adr/ row, … | The deleted execution plans are recoverable from git (37c9f0e → fcf64ba); nothing needs to carry the history forward. |
| high-level-architecture.md | QUESTION | 27 confirmed rewrites applied (JDK labels corrected to the JDK 25 LTS baseline on origin/main, bootstrap DAG de-StructuredTaskScope'd, the §3.1 retraction wrapped with the sanctioned Vale toggle and its report path un-elided, two benchmark … | The superseded "demo product" and "Trojan horse" lead-generation framings removed from §9.1 belong in the private business decision registry at ~/exeris-systems/exeris-business/ as a dated note recording that the Family-product positioning … |

## VERIFY markers (58)

Each is a `<!-- VERIFY(sweep-2026-09): … -->` comment left where the checkout could not settle the
question; the full text lives in the file itself. Every one is a `[DOC DEBT]` item.

- adr/ADR-020-open-core-documentation-mirror-policy.md:29 — Public/private repo enumeration in Context is contradicted, not settled: `exeris-telemetry-spec` is listed here as public open-core and ADR-018 §Consequences ("Publishable open spec"), that repo's own C …
- adr/ADR-020-open-core-documentation-mirror-policy.md:32 — Both enumerations in Context are stale in the same direction: the private list names three repos while `_common.py` PRIVATE_REPOS carries four more that exist on disk as siblings (exeris-business, exeri …
- adr/ADR-020-open-core-documentation-mirror-policy.md:75 — §1 forbids duplicate full-content copies and §2 forbids stubs that replicate content, yet `exeris-kernel-enterprise/docs-open-core/` holds full-content near-copies of `exeris-kernel/docs` — a scripted c …
- adr/ADR-020-open-core-documentation-mirror-policy.md:100 — §5 bullet 3 (no two repos hold full-content copies) has no implementation four months after acceptance.
- adr/ADR-053-sku-composition-manifest-format.md:29 — <!-- VERIFY(sweep-2026-09): founder decision, not an open question of fact. The Context paragraph and the "Zero new dependencies" trade-off both put Jackson 3 on the whole manifest path.
- adr/ADR-002-unified-database-strategy.md:29 — <!-- VERIFY(sweep-2026-09): the >50k writes/sec figure on this line has no report path.
- adr/ADR-004-jdk-26-ea-preview-features-mandate.md:25 — "the Decision below no longer describes the default distributable line… exeris-kernel/pom.xml:113, exeris-kernel/exeris-kernel-build-config/pom.xml:24, exeris-sdk/pom.xml:95, exeris-tooling/pom.xml:112, ex …
- adr/ADR-004-jdk-26-ea-preview-features-mandate.md:35 — "StructuredTaskScope (JEP 525) is still a preview API on JDK 26 GA… The Context bullet above is consistent with this ADR's own --enable-preview mandate and is not withdrawn here; what changed is downstream …
- adr/ADR-004-jdk-26-ea-preview-features-mandate.md:40 — "the Context clause 'JDK 25 lacks JEP 491' contradicts the Virtual Threads bullet in this same ADR's Context, which places JEP 491 in JDK 24.
- adr/ADR-004-jdk-26-ea-preview-features-mandate.md:52 — "obligation 3's 'EA acceptable until GA' has expired on its own terms — JDK 26 reached GA on 2026-03-17… the shipped operator floor is 'Java 25 LTS or newer' (support-matrix.md, README.md §Requirements); o …
- adr/ADR-004-jdk-26-ea-preview-features-mandate.md:55 — "neither enforcement clause in obligation 5 describes a live gate, and the first describes the inverse of one.
- adr/ADR-004-jdk-26-ea-preview-features-mandate.md:68 — "the 'no migration required' clause needs a founder ruling; the scalarisation clause beside it stands (whitepaper.md:40-42).
- adr/ADR-006-spring-free-kernel-boundary.md:12 — last-verified is 2026-09-04 because every claim was checked against source this sweep, but several do NOT match the code and stay unrepaired pending founder approval; rendered Markdown hides these comments so a r …
- adr/ADR-006-spring-free-kernel-boundary.md:24 — Scope binds four repos but only exeris-kernel carries docs/adr/ADR-006.link.md and adr-index.md:118 lists only that repo; adr-conventions rule 5 requires a stub in every consuming repo.
- adr/ADR-006-spring-free-kernel-boundary.md:34 — the '~30 MB' jar-inflation figure has no report path and no figure state; it appears nowhere else in the corpus and is not in CLAIMS.md. Founder decision under rule 8: gate it or drop it.
- adr/ADR-006-spring-free-kernel-boundary.md:49 — obligation 2 lists TelemetrySink among ServiceLoader-discovered providers; the SPI service file is telemetry.TelemetryProvider and sinks come from TelemetryProvider.createSinks(TelemetryConfig).
- adr/ADR-006-spring-free-kernel-boundary.md:51 — obligation 3 restates a boot sequence that exists on neither branch: no KernelBootstrap.bootstrap() (entry point is boot(Runnable)), no Exceptions subsystem, Config resolved via ServiceLoader<ConfigProvider> befo …
- adr/ADR-006-spring-free-kernel-boundary.md:52 — before amending obligation 3, note the intended source is itself inconsistent: BootstrapPhase javadoc puts crypto and telemetry in FOUNDATION while CommunityCryptoSubsystem.phase() returns SERVICES, and bootstrap …
- adr/ADR-006-spring-free-kernel-boundary.md:54 — obligation 4 lists HttpServletRequest among bridged types; no such adapter exists today, ADR-011:49 reserves a narrow *.compat.servlet.* bridge for it, exeris-spring-runtime-web/pom.xml:34 says Phase 2 will add o …
- adr/ADR-006-spring-free-kernel-boundary.md:58 — obligation 5 attributes the Tomcat/Jetty/Undertow/HikariCP bans to PureModeClasspathGuardTest, which asserts none of them; the bans are real but carried by WallIntegrityTest:97, ExerisBootstrapIntegrationTest:135 …
- adr/ADR-006-spring-free-kernel-boundary.md:69 — the '< 5 µs PAQS shed decision' figure is an Enterprise SLO whose only source is exeris-kernel-enterprise/docs/performance-contract.md:209 (stated there as a ≤ bound, and :218-226 records the scheduler is not yet …
- adr/ADR-006-spring-free-kernel-boundary.md:77 — 'Onboarding docs must address this' has been open since 2026-02-22 and no page under exeris-spring-runtime/docs or exeris-kernel/docs explains the Spring-side vs Exeris-side bootstrap phase split.
- adr/ADR-006-spring-free-kernel-boundary.md:98 — opening a ## Amendments section is not a self-contained edit: ADR-085 §25 and adr-conventions rule 7 require the adr-index.md status cell to change in the same PR to 'accepted (2026-02-22, upd.
- templates/README.md:84 — <!-- VERIFY(sweep-2026-09): the closing clause of the Language bullet lets repo-local working notes be written "in any language", while standards/docs-style-guide.md rule 11 (line 44) states "English." with no carve-out and scopes itse …
- rfc/RFC-2026-09-04-docs-guardrails-open-decisions.md:25 — <!-- VERIFY(sweep-2026-09): this RFC cites `docs-guardrails-plan.md` by section (§8 here, §7 under *Risks of the recommendation*) and ADR-085's Engineering Protocol cites the same plan, but no file of t …
- adr/ADR-023-capability-licensing-taxonomy.md:59 — <!-- VERIFY(sweep-2026-09): obligation 1 requires a "## License" section in every cap README.
- adr/ADR-023-capability-licensing-taxonomy.md:74 — <!-- VERIFY(sweep-2026-09): obligation 6 says periodic manual licence audits "substitute, performed quarterly". No audit record exists anywhere under ~/exeris-systems/ and no workflow runs one.
- adr/ADR-024-capability-composition-model.md:77 — <!-- VERIFY(sweep-2026-09): predicate 3 allows resolving a duplicate @Provides by "an explicit `prefers:` directive in the composition".
- adr/ADR-024-capability-composition-model.md:97 — <!-- VERIFY(sweep-2026-09): obligation 3's first clause — "A new `exeris-caps-*` repository scaffold ships with the standard guard set" — has no realization.
- adr/ADR-024-capability-composition-model.md:100 — <!-- VERIFY(sweep-2026-09): obligation 5 requires SKU manifests to pin every cap to an exact version — "no LATEST, no version range broader than a single release".
- adr/ADR-085-documentation-architecture-and-repo-hygiene-standards.md:83 — <!-- VERIFY(sweep-2026-09): §E.16 makes authors declare "File categories touched (A | B | C, SDK-using repos only)" and §J.31 gates "Category-B files edited without regeneration marker", …
- adr/ADR-085-documentation-architecture-and-repo-hygiene-standards.md:118 — <!-- VERIFY(sweep-2026-09): §J.33 patches "the pr-review.md router for every repo" and calls [DOC DEBT] a sibling of [TCK DEBT] in "the existing ladder".
- adr-index.md:123 — marker-vocabulary conflict: ADR-085 §Engineering Protocol item 1, §G.24, adr-conventions.md rule 4, registry_check.py:67 and frontmatter_check.py:98 all say *(content private)*, while registry_check.py:85 (the stubs-table branch) and five ex …
- adr-index.md:124 — rows 071/073/074/077/080/083 link to kernel ADRs present only on `development/0.12.0` (verified absent from `main` and from every tag through v0.11.0).
- adr-index.md:125 — the 085 cross-repo-stubs row required by ADR-085 §Engineering Protocol item 1 cannot be written: no `ADR-085.link.md` exists in any repo yet.
- adr-index.md:126 — rows 081, 082 and 084 read `reserved (… content TBD)` while their content files exist on `development/0.12.0` with header Status ACCEPTED; adr-conventions.md rule 6 requires the row to mirror the file's header table.
- adr-index.md:127 — rows 064 and 066 assert pre-decision code state in the present tense with dead line anchors (`FlowDefinition` keyed solely by `String name`; `resolvePlanForSnapshot :468`; `CoreFailurePolicyTckTest:113` catching `BootstrapException | FailedE …
- adr-index.md:128 — rows 040/049/050 carry the discharged future-tense clause "reaches `main` with the 0.10 release"; the strike is verified and ready but is a registry-row edit outside the three authorised edits.
- b2b-technical-whitepaper.md:64 — §3.2 says "SKUs compose 10–15 capabilities" while §3.3's representative manifests list 7 to 16 named capabilities each (Bot Blocker 7, IDP/PIM/OMS 16).
- b2b-technical-whitepaper.md:147 — the "Commercial positioning" column asserts comparative claims no campaign supports: "Envoy/Kong density and policy parity at sub-Envoy memory and CPU footprint" (API Gateway), "Sub-millisecond P99 failover at the edge" (Edge …
- b2b-technical-whitepaper.md:215 — §4.1's evidence table labels figure states with the benchmark lab's vocabulary (`comparison_eligible`, `exploratory`), while standards/claims-and-evidence.md rule 1 names the permitted states as `citable`, `unartifacted`, or a …
- b2b-technical-whitepaper.md:259 — §4.4 describes perf-box-amd64 as "Hetzner AX-class — AMD x86-64, 16 hardware threads, 64 GB RAM; Falkenstein DE / Helsinki FI".
- b2b-technical-whitepaper.md:295 — the "low five-figure euros" Code Detachment Fee band appears here and again in §6, in a public buyer-facing document.
- b2b-technical-whitepaper.md:330 — §7's Q3 2026 row lists "TRL-4 integration-tested for kernel subsystem set" inside a horizon that closes 2026-09-30.
- cap-author-guide.md:362 — <!-- VERIFY(sweep-2026-09): the "Phase 5" label in the two rows above is the 2026-07-21 gateway-caps implementation plan's Phase 5 — the same phase ADR-053's Engineering Protocol item 1 and `exeris-tooling/ROADMAP.md` (lines 205 and 2 …
- CLAUDE.md:67 — the "TRL-4" label on the planned FFM Bolt v5 Neo4j driver is contradicted inside its own repo. I re-verified every citation: exeris-kernel-enterprise/docs/subsystems/graph.md:8, :55, :93 say "planned TRL-4"; exeris-kernel-enterprise/docs/ROADMAP …
- CLAUDE.md:85 — exeris-kernel/docs/subsystems/bootstrap.md contradicts itself on the phase-start mechanism: its Holy Order block (:213-215) and Diagram 1 (:66,:71) still read "(parallel)", while its own ADR-066 passage (:340-355) records the fork removal and "a …
- CLAUDE.md:87 — exeris-spring-runtime/CLAUDE.md:83 still carries the superseded StructuredTaskScope DAG corrected here. I read the line in full; it is verbatim the old form. Outside this sweep's scope; cross-repo [DOC DEBT].
- CLAUDE.md:93 — the gates these standards cite are not wired into this repository. .github/workflows/ holds only claude-code-review.yml and claude.yml, and `grep -rn "registry_check|frontmatter_check|claude_md_check" .github/` returns nothing (exit 1).
- CLAUDE.md:105 — the Common drift patterns list is load-bearing for two standards: docs-style-guide.md rule 10 seeds Vale existence rules from it by section name, and claims-and-evidence.md rule 4 makes items 10–13 error-level Vale rules citing them by number.
- CLAUDE.md:152 — REWRITTEN from the skeptic's text, whose premise my own edit falsifies. The skeptic wrote "16,301 bytes … 83 bytes of headroom, the size warning does not fire today".
- README.md:31 — REWRITTEN from the skeptic's text, whose premise the working tree falsified while I worked. The skeptic wrote "adr-index.md has no 085 row — the registry's highest number is 084"; adr-index.md is now modified in the working tree and line 115 car …
- CLAUDE.md:121 and :128 — Vale gate (not a VERIFY marker): `<!-- vale Exeris.RetractedFigures = NO -->` … `= YES -->` wrapping drift-pattern items 10–13, per brief rule 4 and step (d).
- high-level-architecture.md:117 — ADR-004 (JDK 26 EA + Preview Features Mandate) is still 'accepted (2025-12-26)' and unqualified in adr-index.md while ADR-066/068/069 moved every distributable repo to JDK 25 LTS and the origin/main poms follow them; founder mu …
- high-level-architecture.md:197 — the '~128-200 MB target RSS baseline (ARM64 edge class)' and 'Full Java on 512 MB IoT gateways' figures have no artefact: the cited kernel whitepaper §4 carries no RSS figure, exeris-benchmarks/docs/edge-rss-baseline.md does no …
- high-level-architecture.md:422 — '>50k RPS' on the §5 API Gateway row has no public report path and no figure state; the only H3 results live in the enterprise-private benchmarks repo, which docs-style-guide rule 6 forbids a public page citing.
- high-level-architecture.md:423 — 'sub-ms failover' on the §5 Edge Proxy row has no measurement in either benchmark repository, and no exeris-sku-edge-proxy repository exists to measure. Placed immediately after the §5 table.
- high-level-architecture.md:450 — exeris-telemetry-spec is described as open-core and open-licensed while the repository is private with no LICENSE and ADR-018 frames the open spec as something Repo C 'can be released' as; founder call whether to publish the re …

## Not applied (109)

Refuted by the adversarial pass, or deliberately left undone.

- Did NOT write any `## Amendments` section. All three refuted proposals were drafted Amendments entries that would have asserted founder-reserved conclusions inside an ACCEPTED record — reclassifying `exeris-telemetry-spec` as enterprise-private, stamping `(pub …
- Did NOT touch line 38's `(public)` parenthetical on `exeris-telemetry-spec`. The evidence is a live contradiction (ADR-018 §Consequences, that repo's CLAUDE.md and README, HLA:432, whitepaper:44 say open; `_common.py` PRIVATE_REPOS, `lychee.toml`, the inventor …
- Did NOT retro-fit ADR-085 §G.26's new `### Non-Goals` / `### Risks and Assumptions` template sections into this 2026-05 record. `adr-conventions.md` rule 8 dates that template addition to 2026-09-04 and reads forward-looking; sweep rule 5 says records are amen …
- Did NOT edit any `public-staged` occurrence (lines 84, 106, 127, 133). All four are the deprecation instrument itself, not live use of the value — line 84 IS the deprecation, 106 its consequence, 127/133 record the registry edit.
- Did NOT edit `adr-index.md`. Its row 119 links relatively into `exeris-telemetry-spec` and is the one live gate failure attributable to this dispute, but ADR-085 §Engineering Protocol item 1 already assigns it to the founder and it falls outside the two edits …
- Did NOT bump `last-verified` to 2026-09-04. `docs-style-guide.md:26` defines the key as the date a human last confirmed the page matches the code; this page demonstrably does not match on the two Context enumerations and carries four unresolved VERIFY markers. …
- Did NOT add a `vale Exeris.RetractedFigures` gate block. The page carries no performance, footprint, throughput or capacity figure at all — nothing to gate, and no retraction text to protect. CLAIMS.md was not needed and was not opened or written to.
- Note on the brief's step (e): this file's bucket is QUESTION, and step (e) says a QUESTION-bucketed file gets nothing beyond frontmatter.
- All three of the skeptic's `confirmed` rewrites were deliberately NOT applied to the body. Each one's own evidence field ends REPORT-ONLY, and the skeptic re-bucketed the page QUESTION; the apply brief's rule (e) says a QUESTION page is not edited beyond front …
- The skeptic's two `refuted` items were not applied and are not reported as findings: the triage's "7–17" replacement for the cap-count range (the IDP row at high-level-architecture.md:324 lists sixteen caps, not seventeen — the real range is 7–16), and the "ca …
- `last-verified` was NOT stamped 2026-09-04. docs-style-guide rule 2 defines the key as the date a human confirmed the page matches the code, and doc-page checklist item 9 repeats it.
- `status: stale` was NOT used. The skeptic's bucket is QUESTION, not DEBT: nothing on the page is unverifiable (every load-bearing artefact was checked against production source), and `stale` would contradict the record's own **ACCEPTED** header table, which ad …
- No `## Amendments` section was created and no `## Non-Goals` / `## Risks and Assumptions` sections were backfilled — both are founder decisions under rule 8 (and rule 5 for the amendment).
- No edit was made to adr-index.md, and no `ADR-053.link.md` stub was created or removed, despite a real registry inconsistency found (see questions[]).
- The item brief's Option-A watch ("if this ADR implies a sub-SKU artefact between capabilities and SKUs, that is a QUESTION") was checked and produced nothing: ADR-053 names exactly two artefacts, the authored `composition.json` and the tooling-emitted `cap-man …
- REFUTED, not applied — ADR-002:37 (post-edit) `* **Graph:** **SQL/PGQ** standard (read) + Recursive CTEs (write) for graph traversals (replacing Neo4j).` The triage's narrowing rewrite was refuted by the skeptic on three grounds and I applied nothing: it is De …
- REFUTED, not applied — the triage's own replacement wordings for both ADR-001 findings. Its saga sentence asserted unconditional durable persistence to PostgreSQL, which `FlowEngineConfig#persistenceEnabled()`, the heap-store fallback and JdbcFlowSnapshotStore …
- Bucket-instruction conflict, resolved and flagged. The skeptic bucketed ADR-002 QUESTION (for the "replacing Neo4j" line, not for the >50k figure), and step (e) of my brief says a QUESTION file gets nothing beyond frontmatter.
- No `<!-- vale Exeris.RetractedFigures = NO/YES -->` gating inserted anywhere. Neither file quotes anything in the CLAIMS.md retraction register (#1–#23) and neither carries a retraction box, so there was nothing to fence.
- Left alone (rule 3 vs rule 5, deliberately): ADR-001's `### 📋 Incremental Expertise Clause` future-tense clauses and both files' discharged `## Engineering Protocol` boilerplate.
- Not touched, verified correct rather than assumed: ADR-002:22 `| **Compliance** | [Strategic Pillar: No-Waste Compute](../../exeris-kernel/docs/whitepaper.md) |`.
- Not touched (out of scope, exeris-docs only): exeris-kernel/docs/modules/01-spi.md:53 still lists `CitadelRepository` in the `*.spi.persistence` allow-column, and exeris-kernel/CONTRIBUTING.md repeats it. Worth a `[DOC DEBT]` item against exeris-kernel.
- No body text was edited at all. The file is bucketed QUESTION and every confirmed finding is record text under rule 5 (the Decision sentence, its five obligations, one Context premise, two Consequences bullets, one Non-Goals bullet, the Engineering Protocol).
- status: stale NOT written, per the skeptic's correction. The brief's "use stale for anything you bucket DEBT" clause does not fire once the bucket is QUESTION; adr-conventions rule 6 requires a record's status to mirror its header table, which reads **ACCEPTED …
- last-verified: 2026-09-04 NOT written. docs-style-guide.md:26 defines the key as the date a human confirmed the page matches the code; this page demonstrably does not match six repositories, so the file's last substantive commit date (2026-05-11) is used and t …
- Four refuted items not applied: (a) the StructuredTaskScope Context bullet was NOT marked as a stale claim — the marker at :35 explicitly says the bullet is not withdrawn; (b) the 2025 History note at the end of the Decision section was left completely untouch …
- No separate markers for the confirmed findings at original lines 33, 34, 52, 58 and 69 (compiler-target and test-JVM-args obligations, the operator-JDK trade-off, the "code that targets older JDKs is rejected" non-goal, and the Engineering Protocol's "Existing …
- adr-index.md was not touched. My brief named no adr-index edits for this file, and the row change ("accepted (2025-12-26)" → whatever the founder rules) is rule-8. The marker at :25 names the row and its line so the two move together.
- exeris-benchmarks was neither read from nor written to, and CLAIMS.md was not needed: `grep -nE '[0-9]+…(%|GB|MB|ms|ops|×)|Axon'` over the file returns nothing — it contains no performance, footprint or throughput figure, and nothing in it withdraws one, so ru …
- No Vale RetractedFigures gating comments were added — there is no retracted or forbidden figure on the page to gate.
- Applied ZERO prose rewrites. Every one of the seven confirmed findings is explicitly non-appliable by the skeptic: five are marked 'NOT an in-place edit (rule 5)' or 'FOUNDER DECISION (rule 8)', one is registry debt outside this file, and one is a bucket-QUEST …
- Did not open a '## Amendments' section, even though five findings resolve to amendment clauses. Two independent blockers: rule 5 (a substantive change is a dated Amendments entry the founder approves, not a sweep edit) and the procedural coupling the skeptic f …
- Did not touch adr-index.md line 118 (the ADR-006 cross-repo-stubs row) or create the three missing docs/adr/ADR-006.link.md stubs in exeris-kernel-enterprise, exeris-sdk and exeris-spring-runtime.
- Did not apply any of the four items the skeptic refuted: (1) the triage's 'servlet API is banned outright on both sides of the seam' wording for obligation 4 — it contradicts ACCEPTED ADR-011:49; (2) the '(sequential)'/'(parallel)' phase annotations and member …
- Did not apply rule 3 (present state, not archaeology) to the Context section ('The original Exeris platform was structured around Spring…', 'That model was abandoned') or to the 'Driven By' header cell.
- Did not use any Vale RetractedFigures gating wrapper: rule 4 is not engaged on this page. I grepped it for Axon, the e2e-shop-order-saga RSS values, the 3.4×/4.7× multipliers, '~60% CPU', '>160 GB', 'Postgres-only', 'replacing Neo4j' and TRL-5..9 — all empty.
- Left status: active (not stale) — the skeptic sustained the QUESTION bucket and made no DEBT re-bucketing, and adr-conventions rule 6 makes a record's status mirror its header table, which reads ACCEPTED.
- Wrote nothing to /home/arkstack/exeris-systems/exeris-benchmarks (read CLAIMS.md only from the scratchpad copy) and ran no mutating git command in any repo.
- templates/ADR-TEMPLATE.md — NOT EDITED AT ALL. Bucket KEEP. Its frontmatter block (lines 1-9, including slug: adr/ADR-NNN) was already in the working tree before this sweep and is correct; I re-parsed it and inserted no second block.
- Three VERIFY markers proposed for the template BODIES (ADR-TEMPLATE, RFC-TEMPLATE, RESEARCH-TEMPLATE) — refuted by the skeptic, not applied.
- The `last-verified: YYYY-MM-DD` placeholder was NOT converted to a real date in any of the three templates, per the item brief. The validator still errors on all three (see question 2); that is the intended outcome, and RESEARCH-TEMPLATE.md now trades its hard …
- `owning-repo: exeris-<repo>` left as a placeholder in all three templates, diverging from the sweep brief's `owning-repo = exeris-docs` rule, for the same reason as the date: a template's frontmatter is copied content, and a literal `exeris-docs` would stamp t …
- templates/README.md got NO frontmatter block — README files are exempt by the sweep brief and by frontmatter_check.py:35-36 (`if name == "README.md": return`). Confirmed live: my validator run over four files reported "Checked **3** files".
- The proposed neutral/precedence pointer under templates/README.md §Conventions — refuted and not applied. The bare `## Conventions` heading states nothing false, and the proposed clause "where the two disagree, the standard wins" is a new precedence rule no so …
- No `status: stale` written anywhere — no file was bucketed DEBT. A template's `status:` is a placeholder for the document the author will write, not a statement about the template, so `stale` would propagate into every record copied from it.
- Rules 3 and 4 (retractions, figures) never engaged on this item and nothing was gated. Re-verified negatives across all four files: `grep -nEi '[0-9]+ *(ms|µs|us|ns|MB|GB|KB|%|×|req|ops)'` exits 1, and Vale with the repo config reports 0 errors (so zero Exeris …
- Rule 6 (English) did not engage: `grep -nP '[ąćęłńóśźżĄĆĘŁŃÓŚŹŻ]' templates/*.md` exits 1 with no output.
- Rule 5 respected: no ADR or RFC record was touched. RFC-TEMPLATE.md is a template, not an RFC, and the one sentence changed there sits inside its authoring-instructions block and rewrites no argument.
- ONE DEVIATION FROM THE SKEPTIC'S MARKER TEXT, and it is a correction, not a liberty. The supplied marker said the plan's other citation was '§7 at line 161'.
- REFUTED FINDING NOT APPLIED — `PresentationMetadata` → `ViewMetadata` at lines 110 and 147 (now 119 and 156) of the marketplace RFC.
- REFUTED FINDING NOT APPLIED — the VERIFY marker claiming `pr-review.md` does not exist. I independently confirmed the file at /home/arkstack/Pobrane/files/pr-review.md (3728 bytes, present).
- REFUTED FINDING NOT APPLIED — the rewrite of line 24 ('every published `pom.xml`' → 'the SDK and tooling `pom.xml`') in the guardrails RFC.
- REFUTED FINDING NOT APPLIED — the `exeris.io (5 schema $id)` → `11` correction in the guardrails RFC's *Data gathered* table. The count is factually right (the skeptic reproduced 11 on both refs) but it is a figure in a record's evidence base; rule 8 reserves …
- NO `status: stale` SET ANYWHERE. Neither file was re-bucketed DEBT — the skeptic moved both from KEEP to QUESTION — so the DEBT rule (f) did not fire. Both carry `status: draft`, mirroring their own header tables per adr-conventions rule 6.
- NO BODY EDITS beyond the single confirmed pointer repair. Rule 5 governs: both files are historical arguments. The five drift statements in the 2026-06-25 RFC and the two wrong figures in the 2026-09-04 RFC were left exactly as written and routed to the founde …
- exeris-benchmarks was not read or touched at all — I had no finding that needed it. Its dirty checkout is untouched, and I never opened the scratchpad CLAIMS.md copy because neither file quotes a benchmark figure (Vale's RetractedFigures rule is silent on both …
- REFUTED, not applied — ADR-023 line 103 (now 115), the `Context-Centric CRM | N/A — cross-cutting cap (exeris-caps-contact-graph)` row.
- REFUTED, not applied — ADR-024 line 200 (now 213), "Tracked as a follow-up — the assertion logic and its golden vector port unchanged; only its module coordinates move." It is the last sentence of `### Migration` inside the **2026-06-25 dated amendment** secti …
- Not applied, out of mandate (rule 8) — adr-index.md rows 023 and 024 both still read `accepted (2026-05-13)`. ADR-023 carries a same-day amendment plus a 2026-08-16 inventory note; ADR-024 carries three amendments through 2026-07-21.
- Not applied, out of mandate — adr-index.md:111's note about ADR-024 line 79 names the assertion's home as "the platform composition runtime", itself superseded by the 2026-06-25 amendment (home is `exeris-sdk-composition-runtime`).
- Not applied, out of mandate — neither ADR carries a `## Amendments` heading; both use named sections with dated parentheticals. `grep -ln "^## Amendments" adr/*.md rfc/*.md` returns nothing repo-wide, so this is house style, not a local defect.
- One deviation from the skeptic's marker text, deliberate: in the ADR-023 obligation-6 VERIFY marker I wrote "the POM licenses block" instead of "the POM <licenses> block", to avoid embedding a raw angle-bracket tag inside an HTML comment. No change of meaning.
- Not touched — cap-license-registry.md and cap-author-guide.md are owned by another agent. Read only; contradictions reported in questions[].
- ALL THIRTEEN of the skeptic's confirmed prose rewrites were NOT applied. The skeptic's own notes rule the file QUESTION and state it explicitly: 'the confirmed list is a proposal sheet for the founder, not a work order … The frontmatter block is the only sweep …
- The line-148 tense fix ('amended by the 0.12 licence change' → 'to be amended by') was NOT applied either, although it is outside Decision text and I verified it independently (neither exeris-kernel/docs/adr/ADR-008-open-core-strategy-and-commoditization-of-of …
- The two VERIFY markers the skeptic REFUTED were not inserted: the 'what did 21 count' marker on line 15 (the figure is fully knowable — 39 remote branches today — so a marker would manufacture doc debt out of a one-token correction) and the §A.5 '10 minutes in …
- No <!-- vale Exeris.RetractedFigures --> gating was added: this document contains no retracted or gated figure. Its numbers are corpus and build-tooling metrics — no saga table, no CPU-waste or allocation magnitude, no Axon-adjacent number, no TRL claim.
- The false clause 'no metadata on any file' (line 25, same sentence as three of the proposals) was left untouched. standards/_inventory/2026-09-inventory.md:49 says '1 of 207 public-core markdown files under docs/ starts with ---'.
- The exeris-benchmarks checkout was not read from or written to. CLAIMS.md was consulted only at the scratchpad copy path given in the brief; the benchmarks working tree is still at its pre-existing 42 dirty entries.
- Lines 61/70/71 (rows ADR-040, ADR-049, ADR-050) — the confirmed strike of "; reaches `main` with the 0.10 release" was NOT applied. I re-verified the finding myself (all three files are in the `v0.10.0` tag, dated 2026-07-02, and on `exeris-kernel` `main`), so …
- The four skeptic-confirmed REPORT-ONLY findings (rows 081, 082, 084 status finalisation; rows 064/066 pre-decision prose; the ADR-031 reservation note's 0.8.0 anchor; the ADR-016/018/019 *(private repo)* vs *(content private)* vocabulary) — all left untouched …
- The two "misaligned" rows the item brief asked me to find are NOT misaligned and were deliberately not fixed — see triageTable and questions[]. Both stray pipes are correctly escaped (`\|`); the file is right and a naive column reader is wrong.
- The skeptic's refuted items were not applied: (a) deleting "(content on the kernel v0.11 train)" from rows 061/062/064/065 — true, non-future-tense provenance that §G.23 asks for; (b) the triage's platform-vs-cross-repo VERIFY marker on the 085 row — premise r …
- No retraction wrapping was needed. `.vale.ini` sets `Exeris.RetractedFigures = NO` and `Exeris.DriftPatterns = NO` for `**/adr-index.md` (it is a data table), and I re-ran Vale after the edits: 0 errors, 13 warnings, 200 suggestions — identical to the pre-edit …
- BUCKET TENSION, DECIDED AND FLAGGED — read this first. The skeptic re-bucketed this page QUESTION, and the brief says a QUESTION page gets frontmatter only.
- REFUTED — "SKUs compose 10–15 capabilities" left as-is. §3.3's manifests are labelled "representative", so a 7–16 count of excerpts refutes nothing about full manifests; no exeris-sku-* repository exists to count against; and the identical sentence sits at hig …
- REFUTED — §7 Q3 2026 TRL-4 row left as-is. The TRL survey checks out (crypto.md alone at TRL-4; events/exceptions/config/transport/graph/security/telemetry/memory at TRL-3; bootstrap.md:524 records the TRL-4 requirement as not yet implemented), but this is a r …
- REFUTED — §7 Q4 2026 row's archaeology clause ("the train carrying it is later than the v0.9 label this row originally used") left as-is.
- REFUTED — §3.3 "Commercial positioning" cells left as-is (Edge Proxy sub-millisecond P99 failover, API Gateway Envoy/Kong parity, IDP hyperscaler parity).
- REFUTED (partially) — §4.2's class names `OffHeapTlsEngine` and `eu.exeris.kernel.core.crypto.*` NOT removed. exeris-benchmarks/docs/tls-zero-copy-benchmark-matrix.md:172 does carry a publication guard ("No internal class names … in public artifacts"), but str …
- REFUTED — the "low five-figure euros" price band NOT deleted (it stands at §5.4 and §6). ADR-023 defers pricing to the private business registry and ~/exeris-systems/CLAUDE.md keeps business decisions out of public docs except descriptively; whether a concrete …
- REFUTED — the JFR "sub-1% CPU overhead" figure NOT deleted. CLAIMS.md carries no entry for it, so rule 8 reserves changing it; claims-and-evidence.md rule 1's own remedy for an unsourced figure is the state `unartifacted`, which keeps the number and labels it. …
- REFUTED — the full Vale terminology pass NOT done. Vale reports 41 Exeris.Terminology hits remaining (45 before; the four I fixed rode along on sentences I was already rewriting for substantive reasons).
- NOT TOUCHED, reported only — line 20's retraction paragraph and lines 190-196's §4.1 retraction box are sacred under rule 4 and were not altered by a single character; line 20 gained only the two sanctioned Vale toggle comments.
- NOT TOUCHED, reported only — line 285's "where C++/Rust was previously the only option" contains "previously" but is a statement about the market, not project archaeology. Neither triage nor skeptic flagged it; left alone.
- NOT TOUCHED — adr-index.md, high-level-architecture.md, the portfolio copies, exeris-kernel/docs/whitepaper.md, and the exeris-benchmarks checkout.
- REFUTED, not applied — cap-author-guide.md §8 row "Manifest is emitted to a *source* root … | SKU scaffold, Phase 5 |": triage wanted the "Phase 5" label stripped as unresolvable.
- REFUTED, not applied — cap-author-guide.md §8 row "No `composition.json` reader … | SKU scaffold, Phase 5 |": same false premise. exeris-tooling/ROADMAP.md:226 states verbatim "The ADR-053 canonical `composition.json` reader is Phase 5 (it ships with the `exer …
- REFUTED, not applied — triage's proposed VERIFY marker claiming "No document in the checkout defines a phase numbering for the SKU scaffold". That sentence is itself false and would have been persisted into the file.
- REFUTED, not applied — cap-license-registry.md "**`financial-ledger`** and the SKU-specific edge-failover cap are named elsewhere in the HLA but are **not** in the §3.2 layer tables …".
- NOT TOUCHED (founder decision, rule 8) — the guide's §5 second 0.7.0 anecdote at the "Naming the BOM is not optional" paragraph ("exeris-caps-cors-policy's first CI run died exactly there, on `exeris-tooling-bom:pom:0.7.0-SNAPSHOT (absent) … 401`").
- NOT TOUCHED (founder decision, rule 8) — Vale terminology rule 10 ("*capability*, not *cap*, in prose") fires roughly 60 times in the guide and 15 in the registry.
- NOT TOUCHED (founder decision, rule 8) — the registry does not disclose its own enforcement gap. ADR-023 records that no CI job reads this file; confirmed (`ls .github/workflows/` in exeris-docs → only claude.yml and claude-code-review.yml; nothing greps `cap- …
- NOT TOUCHED (founder decision, rule 8) — [L2] the guide is arguably two Diátaxis pages. docs-style-guide rule 4 forbids a `howto` that explains; §1 "What a cap is", §6's "Two things worth knowing" and §7's mechanism paragraphs are `explanation` material.
- NO RETRACTION WRAPPERS NEEDED — neither file carries a performance, footprint, throughput or cost number, so claims-and-evidence.md rules 1-2 and CLAUDE.md drift patterns 10-13 do not engage.
- NO OTHER REPO TOUCHED — exeris-benchmarks, exeris-tooling, exeris-sdk and exeris-caps-cors-policy were read only. `git status --short` in exeris-docs shows my two files modified alongside other sweep agents' files; nothing outside exeris-docs was written.
- CLAUDE.md workstream (b) — the reorganisation into claude-md-schema.md's verbatim section order. NOT applied, per the skeptic's bucket split.
- The four refuted CLAUDE.md deletions: the "Reserve the number first" bullet, the "Filename pattern" bullet, the "draft an ADR / check the question shape" paragraph, and the opening "Guardrails for AI assistants" sentence. Left untouched.
- The whole-paragraph rewrite the triage proposed for CLAUDE.md:72. Only the dead parenthetical was deleted. The Config / Exceptions / Security sentences were re-verified word-for-word against exeris-kernel/docs/subsystems/bootstrap.md:218 and exeris-kernel/docs …
- README.md restructure to readme-skeleton.md's six sections, and the collapse of the five ADR-registry bullets at README:31-41 into one linking sentence.
- adr-index.md — not mine, and not touched. Note it is already modified in the working tree by another agent (the 085 row at line 115 plus three VERIFY markers at :123-125).
- The .claude/ tree — outside my two files, so not edited, but it is the last live home of the dead execution-plan reference (see questions[]).
- exeris-benchmarks — never written to and never read from the checkout. Retraction facts were re-derived from the scratchpad CLAIMS.md copy only.
- THE BUCKET-RULE TENSION, DECLARED EXPLICITLY. The skeptic re-bucketed this page REWRITE → QUESTION, and the brief's order-of-work item (e) says a QUESTION page gets no edit beyond frontmatter.
- EVERY REFUTED ITEM WAS DROPPED. Not applied: the triage's VERIFY marker about whether Vale honours a same-line toggle (refuted by measurement — I re-ran it myself: baseline 1 error at 173:373, 0 errors after the wrap, and a control row that still asserts the f …
- NO FOUNDER TERRITORY WAS ENTERED (rule 8). No document deleted, no ADR/RFC renamed, no ADR meaning changed, no new record created, and adr-index.md was not touched at all — including row 25 (ADR-004), which my line-117 VERIFY marker flags but does not edit.
- OUT OF SCOPE, HIGH SEVERITY, NEEDS AN OWNER. I confirm the skeptic's finding independently as a cross-repo hazard, and did not touch it: exeris-kernel/docs/whitepaper.md still asserts the retracted 2026-05-05 saga table as live (RSS figures, thread counts, den …
- DISTRIBUTION NOTE, NO ACTION TAKEN. CLAIMS.md's standing instruction names this file as auto-fetched into the portfolio at `prebuild` (portfolio/src/data/docs/high-level-architecture.md is the fetched copy).
- VALE WARNINGS LEFT AS THEY ARE. 276 warnings at warning level (baseline 272). I diffed the two runs: the four-warning delta is entirely Quarkus.Spelling firing on the project's own vocabulary ('Exeris', 'SKUs', 'SPIs', 'whitepaper') in the sentences I added — …

## Questions for the founder (118)

None were acted on. Each is a rule-8 item: a change to what a record means, a figure the claims
register does not gate, a file rename, or a standards change.

1. Is `exeris-telemetry-spec` public or enterprise-private? Records say open (ADR-018 §Consequences "Publishable open spec"; `exeris-telemetry-spec/CLAUDE.md` "publishable as an open spec for third-party decoders"; its README documents GitHub Packages publication on release; `high-level-architecture.md:432` "wire format, …
2. Should the same ruling widen to both Context enumerations? The private list at line 33 names three repos; `_common.py` carries four more that exist on disk (`exeris-business`, `exeris-showroom`, `migration-tools`, plus telemetry-spec).
3. Does `exeris-kernel-enterprise/docs-open-core/` comply with §1 and §2, or is it a standing violation? It holds full-content near-copies of `exeris-kernel/docs` — 13/13 shared subsystem files differ (http.md 129 vs 388 lines, persistence.md 271 vs 599, transport.md 337 vs 680), plus architecture.md, whitepaper.md, perfo …
4. How should §5's implementation status be recorded? Bullet 3 (identical-full-content detection) has no implementation anywhere; bullet 2's on-disk resolution is skipped as `docs-lint.yml:36` invokes `registry_check.py` (no `--siblings-root`); bullet 1 is covered by no gate because `.link.md` stubs name their target as b …
5. Should `adr-index.md:119` be fixed in this sweep or in the founder pass? It carries the relative link into `exeris-telemetry-spec` that `registry_check.py --siblings-root ..` flags, and ADR-085 §Engineering Protocol item 1 already assigns it to you.
6. Escalation, not a question about this file: `Quarkus.Spelling` is unusable on Exeris prose as configured. On this one file it produced 49 warnings, 49 of 49 false (repo, repos, repo's, ADRs, ADR's, Repo — and across the ecosystem, the founder's own name in every ADR header table), while missing both genuine American-sp …
7. ADR-053 lines 17 and 40 (now 27 and 52) attribute Jackson 3 to the tooling classpath, and that is false. Verified independently today: `grep -rh 'import .*jackson' /home/arkstack/exeris-systems/exeris-tooling/*/src/main/java --include=*.java | sort -u` returns ONLY `com.fasterxml.*` (Jackson 2) — including the emitter …
8. Line 45 (now 57) says SKU manifests are "10–15 cap coordinates". The only enumerated source in the repo is the SKU table at high-level-architecture.md:321-329 (§3.3), where a hand count of every row gives 7–16: Bot Blocker 7, Edge Proxy 8, API Gateway 13, Headless CMS API 14, IDP 16, PIM 16, OMS 16.
9. ADR-053 has no `### Non-Goals` and no `### Risks and Assumptions`, which adr-conventions rule 8 requires under *Consequences*. Verified that this is a timing question and not neglect: `git log -S'Non-Goals' -- templates/ADR-TEMPLATE.md` returns nothing and `git status --porcelain templates/` shows ADR-TEMPLATE.md modif …
10. Registry inconsistency around ADR-053's scope, report-only because adr-index.md is founder-owned. adr-index.md:74 registers ADR-053 with scope `platform` ("stack-level recommendation.
11. Repo-wide, not file-scoped: "cap" appears in prose in ADR-053 three times (line 32 "stable array ordering for the cap set", line 45 "10–15 cap coordinates", line 50 "the cap-coordinate set"), which docs-style-guide rule 10 makes a Vale warning.
12. ADR-002, `## 🏁 The Decision` → `* **Graph:** ... (replacing Neo4j).` — founder ruling required. The kernel Graph SPI is dual-engine (exeris-kernel/docs/subsystems/graph.md:23 transpiles the MATCH DSL to SQL:2023 PGQ *or* Cypher; :133 "The same code runs unchanged on PostgreSQL 18 (SQL/PGQ) and Neo4j"; the Cypher path s …
13. ADR-002:27 (and its VERIFY marker) — was `>50k writes/sec` a stated requirement at decision time (2025-10-15), in which case the `unartifacted` label I added is the correct permanent treatment, or was it quoting a measurement? If the latter, the figure has to go via an Amendments entry, because no write-path campaign e …
14. One ruling wanted for every pre-2026 ADR in this sweep, not just these two: how do rule 3 (present state, not archaeology) and rule 5 (records are amended, not rewritten) resolve on future-tense clauses in accepted records whose predictions are still true — ADR-001's Incremental Expertise Clause and both files' dischar …
15. Fan-out the orchestrator must sequence: the dead `Clean IP & Detachment` → `exeris-kernel/docs/architecture.md` link is systemic. Identical text sits at exeris-docs/adr/ADR-006-spring-free-kernel-boundary.md:11 (another agent's file this sweep, with `, module-boundaries.md` appended) and at exeris-business/business-adr …
16. Should ADR-001's `### 🗄️ Redis: Role & Scope` table row now also name `flow.persistenceEnabled` (exeris-kernel/docs/subsystems/flow.md:14) so a reader can act on the condition I added? I did not add it — it is new material in a record, not a citation repair.
17. THE DECISION ITSELF: is ADR-004 superseded by kernel ADR-066, or does it stay ACCEPTED with a dated '## Amendments' entry rescoping the mandate to the `eu.exeris.preview:*` line? Verified today: six of the nine Maven repos plus exeris-kernel/exeris-kernel-build-config sit at <maven.compiler.release>25</maven.compiler.r …
18. SCOPE OF THE AMENDMENT: the skeptic's proposed single Amendments entry must also cover the five obligations and consequences that got no marker of their own — obligation 1 (compiler target, contradicted by the pom sweep plus exeris-tooling/pom.xml:187-190 <requireJavaVersion>[25,)</requireJavaVersion>, the only build-t …
19. TYPO OR SUBSTANCE: 'JDK 25 lacks JEP 491' (Context, final paragraph) contradicts this ADR's own Virtual Threads bullet, which places JEP 491 in JDK 24. I treated it as substance and changed nothing. If the founder rules it a typo, adr-conventions rule 5 permits an in-place fix; otherwise it is an Amendments entry.
20. DOES THE TITLE SURVIVE AN AMENDMENT? 'JDK 26 EA + Preview Features Mandate' names neither the current baseline nor a live state. Renaming an ADR file is rule-8 and would move adr-index.md:25 and exeris-docs/README.md:24.
21. TEMPLATE BACKFILL: adr-conventions rule 8 requires '### Non-Goals' and '### Risks and Assumptions' under Consequences. This ADR has '### 📋 What is NOT in scope' and no Risks section.
22. OUT-OF-SCOPE BUT WORTH A RULING: exeris-telemetry-spec/pom.xml:78-81 sets <source>21</source><target>21</target><release>21</release> with an explicit <enablePreview>false</enablePreview> under the comment 'Java compiler: release 21, no preview features, module-aware' — a deliberate in-tree contradiction of obligation …
23. FOUNDER — obligation 3 (line 49, the boot DAG). `KernelBootstrap.bootstrap()` and the `Config → Memory → Exceptions → {Security, Persistence} → {Graph, Transport} → {Events, Flow} → READY` sequence do not exist.
24. FOUNDER — obligation 2 (line 47, provider discovery). The list names `TelemetrySink`, but the SPI service file is eu.exeris.kernel.spi.telemetry.TelemetryProvider; I enumerated all META-INF/services entries in exeris-kernel and there is no TelemetrySink file.
25. FOUNDER — obligation 5 (line 56, guard tests). `PureModeClasspathGuardTest` ships in eight modules and its four tests are `pureMode_doesNotImportServletApi`, `_doesNotImportNettyOrReactor`, `_doesNotImportWebFluxServerAbstractions`, `_doesNotImportDispatcherServlet` — all ArchUnit source-import rules.
26. FOUNDER — obligation 4 (line 52, HttpServletRequest). Three sources, three positions, and this one cannot be closed inside ADR-006. No servlet adapter exists in exeris-spring-runtime today; ADR-011:49 (ACCEPTED) reserves a 'Narrow `*.compat.servlet.*` bridge for legacy `HttpServletRequest`' in the Compatibility-Mode co …
27. FOUNDER — the '~30 MB' jar-inflation figure (line 32, Context item 2). Rule 8: CLAIMS.md does not gate it. It appears exactly once in the whole corpus (this line) and has no hit in the 989-line CLAIMS.md copy, so claims-and-evidence rule 1 makes it unartifacted and docs-style-guide rule 9 requires both a report path an …
28. FOUNDER — the '< 5 µs PAQS shed decision' figure (line 68, Positive Outcomes). Rule 8. The PAQS mechanism is real in the open kernel (PaqsScheduler.java, glossary.md:153, subsystems/memory.md:197-212, EX-NET-4006), but the FIGURE is Enterprise-private: its only home is exeris-kernel-enterprise/docs/performance-contract …
29. FOUNDER — Scope vs stubs (header table, line 19). ADR-006 declares a four-repo cross-repo Scope; I verified only exeris-kernel carries docs/adr/ADR-006.link.md, and adr-index.md:118 lists only that repo.
30. FOUNDER — the undischarged onboarding obligation (line 76). 'Onboarding docs must address this' has been open six months and thirteen days. No page under exeris-spring-runtime/docs or exeris-kernel/docs explains the Spring-side vs Exeris-side bootstrap phase split for an operator.
31. SWEEP OWNER — does rule 3 (present state, not archaeology) reach an ADR's Context section? ADR-006's Context is built on 'The original Exeris platform was structured around Spring…' and 'That model was abandoned', and 'Driven By' reads 'Abandonment of the original Spring-as-platform model'.
32. SWEEP OWNER — last-verified semantics. I used 2026-09-04 per the brief (claims verified against source this sweep), but docs-style-guide rule 2 defines the key as 'the date a human last confirmed this page matches the code', and this page does not match the code in five places that stay unrepaired.
33. SWEEP OWNER — exeris-docs/CLAUDE.md's own 'Bootstrap DAG — canonical reference' block carries two errors and will re-seed the drift it exists to prevent: it says SERVICES runs 'parallel via StructuredTaskScope' (removed in v0.11 under ADR-066 per bootstrap.md §2 :317-330) and that 'Security is an L1 Citadel concept (AD …
34. OUT OF SCOPE, recorded so it is not lost — same drift family, one repo over: exeris-spring-runtime/docs/architecture/kernel-integration-seams.md:191/197/209 names KernelBootstrap.start(), KernelBootstrap.bootstrap(configProvider) and KernelBootstrap.shutdown(); none of the three exists on the class.
35. Record-status vocabulary mapping (blocks nothing today, silently mis-stamps records). standards/adr-conventions.md:25 rule 6 says frontmatter `status` "mirrors the header table" and then lists `draft | active | superseded | retracted` — it silently drops `stale`, which _common.py:23 STATUSES and docs-style-guide.md:25 …
36. Validator false positive on the templates — the item brief's own finding, now reproduced after my edits. `python3 .guardrails/orggh/scripts/frontmatter_check.py --mode strict --no-section-check templates/*.md` → three errors, one per template: `last-verified must be YYYY-MM-DD (got 'YYYY-MM-DD')`, plus three `filename …
37. `owning-repo: exeris-<repo>` in all three templates diverges from the sweep brief's `owning-repo = exeris-docs` rule. Left as-is deliberately (a template's frontmatter is copied content), but it is a founder call of the same class as the date placeholder, and it should be settled together with question 2.
38. §G.26 heading text. adr-conventions.md:27 rule 8 names `### Non-Goals` and `### Risks and Assumptions`; the working-tree ADR-TEMPLATE.md writes `### 🚫 Non-Goals` (line 75) and `### ⚠️ Risks and Assumptions` (line 79). Rule 8 is [L2] only so nothing breaks, but a reviewer grepping the literal string will miss them.
39. English-rule conflict for repo-local working notes — the one VERIFY marker I did write (templates/README.md:84). docs-style-guide.md rule 11 says "English." with no carve-out and scopes itself to every file under docs/; ADR-020 §4 sanctions Polish-language working notes in <repo>/docs/refactor-notes/; templates/README. …
40. The retired space-separated / DESIGN- filename convention still ships in three files outside my item, all contradicting adr-conventions.md:16-20 and the ADR_FILE / RFC_FILE regexes at _common.py:26-28: ~/exeris-systems/CLAUDE.md:57-58, exeris-docs/README.md:44-45, and exeris-platform/.claude/skills/exeris-platform-deci …
41. Optional polish, not applied: a NEUTRAL pointer under templates/README.md §Conventions that states only the enforcement fact and invents no precedence, e.g. "[`../standards/adr-conventions.md`](../standards/adr-conventions.md) carries the gated rules for record filenames, registry-row order and amendment logging (`[L1: …
42. RESEARCH-TEMPLATE.md line 87 (was 78) prescribes attaching `results/jmh-*.json` and `results/*.jfr`, and that convention has no instance anywhere in the checkout: `find ~/exeris-systems -maxdepth 6 -name 'jmh-*.json'` returns only exeris-kernel-landin/src/assets/jmh/jmh-core-telemetry-results.json (a landing-site asset …
43. Out of scope but flagged for coherence: adr/ADR-085-documentation-architecture-and-repo-hygiene-standards.md itself has no frontmatter and its Status row reads **PROPOSED** — the standards it makes binding are being applied across this sweep while the ADR is unaccepted. Another agent's item.
44. Out of scope, separate [DOC DEBT]: `.guardrails/run-baseline.sh:18` invokes frontmatter_check.py with `--root docs` guarded by `[ -d docs ]`, and exeris-docs has no docs/ directory (2026-09-inventory.md §1.1 confirms "no `docs/` dir").
45. MARKETPLACE RFC — ADR-024 composition-runtime ownership has moved out from under line 12/21. The Affected Repos row credits `exeris-platform` with 'the composition runtime that already exists per ADR-024', but ADR-024's own 2026-06-25 amendment relocates it: adr/ADR-024-capability-composition-model.md:155 ('Composition …
46. MARKETPLACE RFC — the build gate's condition (1) has half fired. Lines 25/48/116 (now 34/57/125) state that no `exeris-caps-*` repo exists and that the first is an H1-2027 target. `exeris-caps-cors-policy` was scaffolded 2026-08-16 and has an emitted cap manifest as of 2026-08-18; its pom is on disk.
47. MARKETPLACE RFC — the sibling presentation RFC is no longer IN-REVIEW. Line 103 (now 112) of the marketplace map marks it so; it is ACCEPTED with Date Closed 2026-06-25 (exeris-sdk/docs/rfc/RFC-2026-06-25-presentation-front-model.md:5,8), and its IR has since shipped (`ViewMetadata`, `@since 0.8.0`).
48. MARKETPLACE RFC — `PresentationMetadata` at lines 119 and 147 (now 156) is a correct 2026-06-25 quotation of the sibling RFC that was renamed to `ViewMetadata` hours later. `ViewKind.FRAGMENT` does exist today (ViewKind.java:20), so a swap would be true — but it would rewrite a snapshot.
49. GUARDRAILS RFC — two figures on the page are confirmed wrong and only the founder may change them. (a) `exeris.io (5 schema $id)` in the *Data gathered* table is 11 on disk (10 under schemas/ plus runtime/profiles/guided-run-profile.schema.json; a 12th occurrence is a $ref).
50. GUARDRAILS RFC — `ADR-DOCS` is still an unreserved placeholder at Target ADR(s) (line 9, now 18) and in the Decision Record (line 169, now 179), while adr/ADR-085-…md exists untracked and `grep -n '085' adr-index.md` returns no match.
51. GUARDRAILS RFC — the Decision Record is open (`Outcome: —`, `Date: —`) and line 170 (now 180) says 'Nothing remains open for the founder except accepting this RFC and reserving the ADR number.' Closing it is the founder's act, not a sweep edit.
52. STANDARDS CONFLICT worth settling before the sweep stamps more files: the 196-character `title` on the marketplace RFC. I set title = the exact H1 because the brief mandates it, and frontmatter_check.py enforces no relationship between the two (presence only) — but templates/RFC-TEMPLATE.md, added to the working tree t …
53. STANDARDS CONFLICT that will recur on every record another agent buckets DEBT: the brief prescribes `status: stale` for DEBT, while adr-conventions rule 6 says a record's `status` mirrors its own header table. On a DRAFT record both cannot hold.
54. DEFECT IN AN EXEMPT SOURCE, for a founder pass: standards/_research/2026-09-generator-spike.md contradicts itself on the figure this RFC quotes at line 66 (now 76). Its comparison table (line 84) says `21 / 21`; its prose (line 59) says 'the 17 `<` and 6 `{}` prose cases' (= 23).
55. ADR-023 obligation 1 vs. the first cap repo (VERIFY marker at ADR-023:59). exeris-caps-cors-policy names Apache-2.0 in README prose (README.md:5) but has no `## License` heading; LICENSE and pom.xml:27-33 both say Apache 2.0.
56. ADR-023 obligation 6's quarterly manual audit (VERIFY marker at ADR-023:74). No audit record exists under ~/exeris-systems/ and no workflow runs one. The nearest live mechanism is a per-PR L2 review instruction in one repository.
57. ADR-024 predicate 3's `prefers:` directive (VERIFY marker at ADR-024:77). No such key exists in any repo, and ADR-053 fixed the manifest format as JSON, so the YAML-shaped spelling cannot be right. Is the directive still intended, and under what JSON key?
58. ADR-024 obligation 3's cap-repo scaffold clause (VERIFY marker at ADR-024:97). cap-author-guide.md §8: "No archetype | ADR-024 promises `mvn archetype:generate` for cap repos. It does not exist". Retire the clause now that ADR-055 moved the gate into the pipeline, or keep it pending the archetype?
59. ADR-024 obligation 5's version-pinning discipline (VERIFY marker at ADR-024:100). Nothing enforces it: no canonical composition.json reader, and exeris:verify-capabilities reads cap-manifest.json rather than the authored SKU manifest. Still live? Which gate will carry it?
60. `exeris-caps-contact-graph` described as a "cross-cutting cap" while the registry and HLA §3.2 place it in Layer 5 and Layer 7 is literally named "Cross-cutting". Five sites share the adjective: ADR-023:115 (was :103), HLA:407, HLA:415, whitepaper:139, whitepaper:321.
61. ADR-024's 2026-06-25 amendment, `### Migration` (now line 213). The relocation from `exeris-platform-composition-runtime` to the SDK-side module is complete and verified. If that should be recorded, it needs a dated entry or an Engineering-Protocol line, not an edit to the amendment that ordered it.
62. adr-index.md registry hygiene: rows 023 and 024 both read `accepted (2026-05-13)` with no `upd.` suffix despite four amendments between them; the cross-repo-stubs row for 024 lists only `exeris-platform`, but stubs also exist at exeris-sdk/docs/adr/ADR-024.link.md and exeris-caps-cors-policy/docs/adr/ADR-024.link.md; a …
63. Cross-repo contradictions found while verifying (files owned by other repos, not touched): (1) exeris-sdk/docs/adr/ADR-024.link.md:38 still says the generated call site is pending — KernelApplicationGenerator.java:138 and :317-319 say it ships.
64. Watch-item, deliberately not flagged in the file: ADR-024's 2026-06-17 amendment (line 137 post-edit) says "The open kernel is Apache 2.0 + Commons Clause". That is still true on `exeris-kernel` main (LICENSE:17 "TIER 1 — COMMUNITY (Apache License 2.0 + Commons Clause)"); the relicense lives only on the 0.12 developmen …
65. Q1 — line 15, branch count (figure, ungated → founder). The Status cell says '21 remote branches'. I re-measured today: `git branch -r | grep -v 'HEAD ->' | wc -l` = 39, and .git/FETCH_HEAD is dated 2026-09-04 12:49 so the refs are current.
66. Q2 — line 25, corpus size scope (figure, ungated → founder). '~3.3 MB of Markdown spread over fourteen repositories' attaches a public-core-only figure to all fourteen repos, understating the corpus by an order of magnitude.
67. Q3 — line 25, Conventional Commits band (figure + dropped fence → founder). '67–96 %' silently drops the qualifier that makes it true. Inventory:110 reads '67–96 % **where it matters**'; the unfenced spread at inventory:51 is 'tooling 96 %, sdk 86 %, platform 86 %, caps 80 %, ai-bridge 78 %, docs 76 %, spring-runtime 6 …
68. Q4 — line 25, subject length (figure, ungated → founder). 'subjects that run to 130 characters' appears in no source: grep for 130/131/184 over the inventory, the RFC and both research files finds no '130 characters'. Inventory:53 says 'tooling max 131, benchmarks max 184'.
69. Q5 — line 25, doclint scope (figure scope, ungated → founder). '~160 findings … in the kernel SPI' mis-scopes the figure. Primary source, read directly: exeris-kernel/pom.xml:555-557 — 'this tree currently has ~160 doclint findings **across spi, core and community**'; the <doclint>none</doclint> it explains sits at pom …
70. Q6 — line 29, link-resolution claim (figure, ungated → founder). 'Docusaurus needs no plugins to resolve the corpus's 489 relative links' overstates the spike. standards/_research/2026-09-generator-spike.md:82 records '424 / 489 equivalent (native)', :88 records '0' plugins, :59 records the default MDX build compiling …
71. Q7 — line 120 (now 130), URL space (figure + self-contradiction → founder). '[+] One URL space for fourteen repos' contradicts §A.3 of this same ADR (now line 45): 'sources.yml may list only repos whose visibility is public under ADR-020.
72. Q8 — line 129 (now 139), migration cost (figure scope, ungated → founder). '[-] ~160 kernel SPI doclint findings' is the second instance of Q5's mis-scope, and here it sizes migration cost. §F.21 turns failOnWarnings=true on exeris-kernel-spi only; no source states the SPI-only subset.
73. Q9 — line 131 (now 141), build time (figure with no report path → founder). '47 s for 123 pages cold' is sound as a number (spike:85 gives 46.8 s, measured on Node 22.22.2 with caches deleted, spike:43) but carries no report path and no figure state, which docs-style-guide.md rule 9 and claims-and-evidence.md rule 1 bo …
74. Q10 — §E.16 and §J.31, the undefined Category A/B/C taxonomy (Decision text → founder). Marked in the file at line 83. I re-ran the search independently: case-insensitive `categor(y|ies) ?[-_ ]?(a|b|c)` and 'file categor' across .md/.java/.ts/.yml/.yaml/.json in every exeris-* repo public and private, plus ~/.claude.
75. Q11 — §J.31, the regeneration marker (Decision text; ADR wording is CORRECT, do not 'fix' it). ADR-085's phrase 'regeneration marker' is accurate and must NOT be changed to `@Generated`.
76. Q12 — §J.33, the pr-review.md router (Decision text → founder). Marked in the file at line 118. Independently confirmed: `find ~/exeris-systems -name 'pr-review*'` returns only .guardrails/orggh/pr-review.patch.md (staged today) and budgetHQ's .claude/commands/pr-review-bhq.md (a separate product namespace, out of regi …
77. Q13 — line 148 (now 158), tense (Cross-references, one word → founder per the skeptic's ruling; I would otherwise have applied it). 'ADR-008 …, ADR-023 … — amended by the 0.12 licence change' states a future event in the past tense.
78. Q14 — registry coordination, not a defect in this file. adr/ADR-085 still has no row in adr-index.md (the registry stops at 084, adr-index.md:105) and its own Engineering Protocol item 1 and §G.23 require the row before the file merges.
79. Q15 — cross-document consistency, for whoever holds rfc/RFC-2026-09-04-docs-guardrails-open-decisions.md. Two items there follow from this file: :23 carries the same '~3.3 MB in the public core repos' that ADR-085 inherited (see Q2), and :29 asserts a '[HARD BLOCK] … [TCK DEBT] … [STYLE]' severity ladder that exists no …
80. MARKER VOCABULARY (blocks nothing, but bakes in a convention). Edit (A) had two defensible targets and the binding texts disagree. ADR-085 §Engineering Protocol item 1 names this exact line and says *(content private)*; §G.24, adr-conventions.md rule 4, registry_check.py:67 and frontmatter_check.py:98 agree.
81. `pending merge` ON SIX ROWS. Edit (B) added "**not yet in a GA release**" to rows 071 and 077 — true, and it normalises them against 073/074/080/083. It does NOT discharge ADR-085 §G.23 / §Engineering Protocol item 1, which ask for status `pending merge (development/0.12.0)` on all six (or the docs merged to `main`).
82. ROWS 081, 082, 084 ARE STALE-BY-STATUS. All three read `reserved (… content TBD)` with no Link cell, but the content files exist on `development/0.12.0` with header Status **ACCEPTED**: ADR-081 (128 lines, dated 2026-08-30), ADR-082 (165 lines, 2026-09-01), ADR-084 (329 lines, 2026-09-02) — each date matching its own r …
83. ROWS 064 AND 066 ASSERT PRE-DECISION CODE STATE IN THE PRESENT TENSE. Four claims, all verified false on BOTH `main` and `development/0.12.0`: (1) row 064 — "`FlowDefinition` is keyed solely by `String name` today (`…/FlowDefinition.java:34`)"; the record is `(String name, int version, …)` on both branches, and `exeris …
84. THE ADR-031 RESERVATION NOTE'S ANCHOR IS THREE RELEASES STALE. The blockquote says "As of 0.8.0 (shipped 2026-06-03) the kernel has **no** tracing logic at all …".
85. LINES 61/70/71 (rows 040/049/050): may I strike the discharged clause? Each ends `(content on the kernel v0.10 train; reaches \`main\` with the 0.10 release)`. The second half is a future-tense promise about an event that happened: all three files are in the `v0.10.0` tag (2026-07-02) and on `main`.
86. ADR-085's OWN STATUS. The row I added reads `proposed (2026-09-04)`, mirroring the ADR header per adr-conventions.md rule 6. But `standards/README.md:12` already says "The standards that ADR-085 makes binding", and every file under `standards/` opens "Binding per ADR-085 §…".
87. THE 085 CROSS-REPO-STUBS ROW cannot be written yet. ADR-085 §Engineering Protocol item 1 requires it to list `ADR-085.link.md` in exeris-kernel, exeris-sdk, exeris-spring-runtime, exeris-tooling, exeris-platform, exeris-ai-bridge, exeris-benchmarks and exeris-caps-cors-policy, plus private-repo markers for the enterpri …
88. CROSS-REPO, URGENT, OUT OF SCOPE HERE — the 2026-08-27 retraction is only half done. exeris-kernel/docs/whitepaper.md on `main` still asserts ">160GB" at line 16 and still carries the full retracted 2026-05-05 saga table at line 136 (459 MB, ~217/~221 threads, 3.4×/4.7×, "both Axon stacks reported 0%"), citing "B2B tec …
89. Does the founder accept the line-202 replacement? A row asserting "319–381 MB across forced GC while idle … 341 MB / 39.7 core-seconds under load … 322 MB for the entire in-process arm" was published fifteen lines below the label table that maps "server-backed" to "The Axon-Framework arms", on the metric CONTRACT-v2-IM …
90. May a concrete price band ("low five-figure euros for SMB-tier SKUs") stand in a public buyer-facing document? It appears at §5.4 line 293 and §6 line 302. ADR-023 states it is silent on pricing and defers to the private business registry; ~/exeris-systems/CLAUDE.md says public docs refer to business policy descriptive …
91. Should §3.3's "Commercial positioning" column keep its comparative claims (Envoy/Kong parity, sub-millisecond P99 edge failover, hyperscaler IDP page-throughput parity)? No exeris-sku-* repository exists and exeris-benchmarks has no edge/gateway/failover scenario.
92. Should §7's Track A horizons be re-based? exeris-docs/CLAUDE.md drift pattern 13 already asks for this pass. Q3 2026 (closing 2026-09-30) claims "TRL-4 integration-tested for kernel subsystem set" while only crypto.md is at TRL-4; the Q4 row still carries an archaeology clause about the v0.9 label it originally used, w …
93. Does "SKUs compose 10–15 capabilities" (§3.2) move? §3.3's representative manifests span 7 to 16. Any change is a four-file edit — this file, high-level-architecture.md:190, and both auto-fetched portfolio copies.
94. Does exeris-benchmarks/docs/tls-zero-copy-benchmark-matrix.md's B5 publication guard ("No internal class names … in public artifacts") bind §4.2, given that OffHeapTlsEngine.java lives in the PUBLIC exeris-kernel repo? If yes it must be applied to the whole section (§4.2 prose, the engine table at line 238, and the Ent …
95. Should §3.2's capability inventory also carry a running total (54 caps, 3 / 50 / 1) to match ADR-023's amendment and high-level-architecture.md:444? I added the four missing layer-7 rows but no total — ADR-023 frames the total as a two-document synchronisation that surfaces contradictions, which is a founder call about …
96. REGISTRY DISCREPANCY, not mine to touch (rule 8): adr-index.md:36 records ADR-015 as `proposed (2026-05-06)` while exeris-tooling/docs/adr/ADR-015-codegen-emission-strategy.md's own header reads "**Status:** Accepted *(merged 2026-05-09 in #12)*".
97. COORDINATION: the "Trojan horse" archaeology I removed at line 370 has a twin at high-level-architecture.md:533, and both files are auto-fetched into portfolio/src/data/docs/ at `prebuild`. The HLA fix belongs to another agent and should land in the same PR, or the phrase reappears downstream on the next build.
98. COUPLING THAT MUST BE RESOLVED IN THIS SWEEP — adr/ADR-024-capability-composition-model.md line 97 carries a VERIFY marker (added by the concurrent ADR agent) that quotes my §8 archetype row verbatim, including the word it used to contain: 'cap-author-guide.md §8 records "No archetype | ADR-024 promises `mvn archetype: …
99. The guide now leads the only reference cap. After the version bump it says SDK 0.11.0 / tooling 0.8.0, while exeris-caps-cors-policy/pom.xml:44,50 still pins 0.10.0 / 0.7.0. Editing that repo is outside this sweep.
100. Vale terminology rule 10 ("*capability*, not *cap*, in prose") versus the shipped strings. The code emits "cap" (CapTierWallException's literal "Cap-tier Wall violated", exeris-sdk-composition-lifecycle's POM description "cap authors compile against exeris-sdk-annotations"), and ADR-024/ADR-055/adr-index.md use it thro …
101. Should the registry disclose that nothing enforces it? ADR-023 records that no CI job reads cap-license-registry.md, and I confirmed it. A reader currently cannot tell that the "Derived, not retyped" rule is convention-only. Adding that line is an editorial addition to a registry — founder call.
102. Is the gateway-caps implementation plan (2026-07-21) meant to live in exeris-docs? Three documents reference its phase numbering (ADR-053:78, exeris-tooling/ROADMAP.md:205 and :226) and my §8 rows depend on it, but the plan document is not in the checkout, and ADR-085's own Engineering Protocol now uses "Phase 5" for a …
103. Should the guide be split into a `howto` and an `explanation` page (docs-style-guide rule 4)? §1, §6's "Two things worth knowing" and §7 are explanation material inside a page now typed `howto`.
104. ADR-085 acceptance. Its header table reads PROPOSED and its frontmatter `status: draft`, yet standards/README.md already opens "The standards that ADR-085 makes binding for every Exeris repository." Until the founder accepts it, both of my files describe standards/ without the word "binding".
105. CLAUDE.md schema reorganisation (workstream b). Four rulings needed before it can be applied: (i) does the H1 change to `# exeris-docs: <one-line mission>`? claude_md_check.py does not inspect the H1, so nothing forces it — and `title` in the new frontmatter mirrors the H1 as it stands.
106. The `~50` capability figure. cap-license-registry.md's totals table sums to 54 while CLAUDE.md and the HLA both say `~50`. I added no new count and left both figures alone — changing an ungated figure is a founder decision.
107. The 2026-05-13 exeris-spring-runtime bootstrap-DAG correction: its parenthetical is now deleted from CLAUDE.md, but the drift it claimed to have fixed is still live at exeris-spring-runtime/CLAUDE.md:83. Cross-repo [DOC DEBT] — who files it?
108. The .claude/ tree still instructs agents to update an "execution-plan §6 reconciliation" that has not existed since 2026-05-18: .claude/agents/exeris-docs-router.md (5 references), exeris-docs-architect.md (3), exeris-docs-implementer.md (4), plus exeris-docs-task-classifier/SKILL.md, exeris-docs-routing-planner/SKILL. …
109. b2b-technical-whitepaper.md:16 still lists Context-Centric CRM alongside the seven SKUs — the same defect I fixed at README:13. That file belongs to another agent in this sweep; the two edits should land together.
110. Branch state: origin/main is at d135264 ("docs: record the cross-repo layer build order (#88)") and adds a root-level layer-build-order.md that is absent from docs/sweep-2026-09. I did NOT add a Top-level-artifacts row for it. If the sweep rebases, that row is needed.
111. §3.1 'Edge sovereignty' — the '~128–200 MB target RSS baseline (ARM64 edge class)' and 'Full Java on 512 MB IoT gateways' figures have no artefact and CLAIMS.md does not gate them.
112. §5 API Gateway row — '>50k RPS' has no public report path. No io_uring or HTTP/3 campaign exists in exeris-benchmarks; the H3 results live in exeris-benchmarks-enterprise, which is private, so docs-style-guide rule 6 forbids this public page citing a path into it.
113. §5 Edge Proxy row — 'sub-ms failover' has no measurement in either benchmark repository, and there is no exeris-sku-edge-proxy repository to measure (confirmed against the GitHub org listing). Drop it, or schedule the campaign?
114. ADR-004 (JDK 26 EA + Preview Features Mandate) is still 'accepted (2025-12-26)' and unqualified in adr-index.md row 25, and mandates release 26 with a build failure below it — while kernel ADR-066, spring-runtime ADR-068 and SDK ADR-069 moved the distributable line to JDK 25 LTS and the origin/main poms agree (kernel, …
115. exeris-telemetry-spec is presented on this page as open-core and 'Open-licensed', but the repository is PRIVATE (GitHub org listing), holds no LICENSE file, is classified enterprise-private in the registry, and ADR-018 says only that Repo C *can be* released as an open-spec artefact.
116. Diátaxis type of this page (docs-style-guide rule 4, `[L2]`). I set `type: explanation`, which is right for the reasoning, but the page also carries large reference tables (54 cap rows, seven SKU manifests, the open-core split).
117. Minor prose call on the bootstrap paragraph (line 172). The confirmed threading sentence was inserted exactly where the skeptic composed it — between 'is not a Subsystem in the DAG' and '`Exceptions` is not a Subsystem layer either' — which puts one sentence between 'either' and its referent.
118. Ownership needed for the cross-repo retraction leak: exeris-kernel/docs/whitepaper.md still asserts the retracted 2026-05-05 saga table (CLAIMS.md register #23, withdrawn 2026-08-27) as live. Outside this item's file scope and outside this repository.

## Validator findings judged wrong

1. **`frontmatter_check.py` on `templates/*-TEMPLATE.md`.** The three templates carry a literal
   `last-verified: YYYY-MM-DD` placeholder, which is correct for a template and which the checker
   reports as `last-verified must be YYYY-MM-DD (got 'YYYY-MM-DD')`. It is not reachable in CI —
   the bundle prescribes `extra-paths: "adr rfc *.md"` for this repo, which excludes `templates/` —
   so it surfaces only when templates are linted explicitly. Either the checker exempts
   `*-TEMPLATE.md`, or templates move under a `_`-prefixed directory, or templates stay unlinted
   and the gap is accepted. A founder call; nothing was changed to work around it.
2. **`Quarkus.ConsciousLanguage` on `rfc/RFC-2026-09-04-…:180`.** The rule asks for "allowlist"
   instead of "whitelist", but the word is the subject of the open question on that line, not a
   descriptive choice. Left as written.
