# ADR Index — Exeris Ecosystem

**Single numbering namespace** across all Exeris repositories. ADR files live next to the code they describe; this index is the canonical lookup (number → owning repo + link).

## Rules

1. **Tech-side ADRs only.** Business decisions (legal / IP / financial / procurement) are kept in a separate, private decision registry maintained outside this repository. Refactoring-only ADRs do not enter the registry.
2. **Number reservation.** A number is reserved by a PR to this file before the ADR-content PR merges.
3. **Visibility.** Per ADR-020.
   - `public` — content lives in a public open-core repo.
   - `enterprise-private` — content lives only in a private enterprise repo. Number is publicly registered here, content is not.
4. **Scope.**
   - `per-repo` — bounded to one repo.
   - `cross-repo` — affects multiple repos. Owning repo holds the authoritative copy; every other affected repo has a `docs/adr/ADR-NNN.link.md` stub.
   - `platform` — stack-level recommendation. Owning repo: `exeris-docs`.
5. **Routines and reviewers cite by number**, not by path.

## Index

| #   | Title                                                                                    | Owning repo                  | Scope                | Visibility         | Status                                    | Link                                                                                                                                                                        |
|-----|------------------------------------------------------------------------------------------|------------------------------|----------------------|--------------------|-------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 001 | Cloud Native & Agnostic Infrastructure Strategy                                          | exeris-docs                  | platform             | public             | accepted (2025-10-10)                     | [adr/ADR-001 …](adr/ADR-001-cloud-native-and-agnostic-infrastructure-strategy.md)                                                                                           |
| 002 | Unified Database Strategy (PostgreSQL 18)                                                | exeris-docs                  | platform             | public             | accepted (2025-10-15)                     | [adr/ADR-002 …](adr/ADR-002-unified-database-strategy.md)                                                                                                                   |
| 003 | Entity-First Development Strategy                                                        | exeris-sdk                   | sdk/dev-paradigm     | public             | accepted (2025-11-12)                     | [exeris-sdk/docs/adr/ADR-003 …](../exeris-sdk/docs/adr/ADR-003%20Entity-First%20Development%20Strategy.md)                                                                  |
| 004 | JDK 26 EA + Preview Features Mandate                                                     | exeris-docs                  | platform             | public             | accepted (2025-12-26)                     | [adr/ADR-004 …](adr/ADR-004-jdk-26-ea-preview-features-mandate.md)                                                                                                          |
| 005 | JFR-First Telemetry Strategy                                                             | exeris-kernel                | kernel/observability | public             | accepted (2026-02-22)                     | [exeris-kernel/docs/adr/ADR-005 …](../exeris-kernel/docs/adr/ADR-005-jfr-first-telemetry-strategy.md)                                                                       |
| 006 | Spring-Free Kernel Boundary (The Wall)                                                   | exeris-docs                  | cross-repo           | public             | accepted (2026-02-22)                     | [adr/ADR-006 …](adr/ADR-006-spring-free-kernel-boundary.md)                                                                                                                 |
| 007 | Next-Gen Runtime Architecture                                                            | exeris-kernel                | kernel/runtime       | public             | accepted (2025-12-11, upd. 2026-02-22)    | [exeris-kernel/docs/adr/ADR-007 …](../exeris-kernel/docs/adr/ADR-007-next-gen-runtime-architecture.md)                                                                      |
| 008 | Open-Core Strategy & Commoditization of Off-Heap TLS                                     | exeris-kernel                | strategy             | public             | accepted (2026-03-05)                     | [exeris-kernel/docs/adr/ADR-008 …](../exeris-kernel/docs/adr/ADR-008-open-core-strategy-and-commoditization-of-off-heap-tls.md)                                             |
| 009 | HTTP Codec module                                                                        | exeris-kernel                | kernel/transport     | public             | accepted (2026-03-10, revised 2026-03-13) | [exeris-kernel/docs/adr/ADR-009 …](../exeris-kernel/docs/adr/ADR-009-http-codec-module.md)                                                                                  |
| 010 | Host Runtime Model                                                                       | exeris-spring-runtime        | spring               | public             | accepted (2026-03-13)                     | [exeris-spring-runtime/docs/adr/ADR-010 …](../exeris-spring-runtime/docs/adr/ADR-010-host-runtime-model.md)                                                                 |
| 011 | Pure Mode vs Compatibility Mode                                                          | exeris-spring-runtime        | spring               | public             | accepted (2026-05-08 — matching the ADR file's own `Date` field; single decider, decision made at draft time and ratified on merge of `exeris-spring-runtime#9` (2026-05-09). The `PROPOSED` label was drift, not a deferred decision: it read as "these requirements are not in force yet" while the Pure/Compatibility split was already enforced by guard tests. Corrected in the ADR file by `exeris-spring-runtime#57` on 2026-08-05; this row carried the same drift until 2026-08-08, and led with 2026-03-22 — a date predating the file — until the same pass) | [exeris-spring-runtime/docs/adr/ADR-011 …](../exeris-spring-runtime/docs/adr/ADR-011-pure-mode-vs-compatibility-mode.md)                                                    |
| 012 | Security Trust Model Upgrade                                                             | exeris-kernel                | kernel/security      | public             | accepted (2026-03-31)                     | [exeris-kernel/docs/adr/ADR-012 …](../exeris-kernel/docs/adr/ADR-012-security-trust-model-upgrade-for-resource-server-validation-and-fail-closed-runtime.md)                |
| 013 | Distributed Saga State Distribution Model                                                | exeris-kernel                | kernel/saga          | public             | accepted (2026-05-02)                     | [exeris-kernel/docs/adr/ADR-013 …](../exeris-kernel/docs/adr/ADR-013-distributed-saga-state-distribution-model.md)                                                          |
| 014 | RequiresRole Compile-Time RBAC Generation                                                | exeris-kernel                | kernel/security      | public             | accepted (2026-05-02)                     | [exeris-kernel/docs/adr/ADR-014 …](../exeris-kernel/docs/adr/ADR-014-requiresrole-compile-time-rbac-generation.md)                                                          |
| 015 | Codegen Emission Strategy                                                                | exeris-tooling               | tooling              | public             | proposed (2026-05-06)                     | [exeris-tooling/docs/adr/ADR-015 …](../exeris-tooling/docs/adr/ADR-015-codegen-emission-strategy.md)                                                                        |
| 016 | HTTP/3 Benchmarking — Enterprise-Only Track                                              | exeris-benchmarks-enterprise | benchmarks           | enterprise-private | accepted (2026-03-23)                     | `exeris-benchmarks-enterprise/docs/ADR-016-HTTP3-BENCHMARKING-ENTERPRISE-TRACK.md` *(private repo)*                                          |
| 017 | JDBC Compatibility Scope for ExerisDataSource                                            | exeris-spring-runtime        | spring/persistence   | public             | accepted (2026-04-07)                     | [exeris-spring-runtime/docs/adr/ADR-017 …](../exeris-spring-runtime/docs/adr/ADR-017-jdbc-compact-scope.md)                                                                 |
| 018 | Observability Tooling Repo Split                                                         | exeris-kernel-enterprise     | cross-repo           | enterprise-private | accepted (2026-03-28)                     | `exeris-kernel-enterprise/docs/adr/ADR-018 Observability Tooling Repo Split.md` *(private repo)*                                     |
| 019 | Enterprise HTTP/1.1 & HTTP/2 I/O Strategy                                                | exeris-kernel-enterprise     | enterprise/transport | enterprise-private | accepted (2026-04-07)                     | `exeris-kernel-enterprise/docs/adr/ADR-019 Enterprise HTTP IO Strategy.md` *(private repo)*                                          |
| 020 | Open-Core Documentation Boundary & Cross-Repo Mirror Policy                              | exeris-docs                  | platform             | public             | accepted (2026-05-05)                     | [adr/ADR-020 …](adr/ADR-020-open-core-documentation-mirror-policy.md)                                                                                                       |
| 021 | Gateway-Class Workloads Out of Compatibility Scope                                       | exeris-spring-runtime        | spring               | public             | accepted (2026-05-09)                     | [exeris-spring-runtime/docs/adr/ADR-021 …](../exeris-spring-runtime/docs/adr/ADR-021-gateway-class-workloads-out-of-compatibility-scope.md)                                 |
| 022 | Persistence SPI Extension — Instant Binders and Readers                                  | exeris-kernel                | kernel/persistence   | public             | accepted (2026-05-11)                     | [exeris-kernel/docs/adr/ADR-022 …](../exeris-kernel/docs/adr/ADR-022-persistence-spi-extension-instant-binders.md)                                                          |
| 023 | Capability Licensing Taxonomy — `community` / `commercial` / `enterprise-private`        | exeris-docs                  | platform             | public             | accepted (2026-05-13)                     | [adr/ADR-023 …](adr/ADR-023-capability-licensing-taxonomy.md)                                                                                                               |
| 024 | Capability Composition Model — `@Provides` / `@Requires` / Build-Time Validation         | exeris-docs                  | platform             | public             | accepted (2026-05-13)                     | [adr/ADR-024 …](adr/ADR-024-capability-composition-model.md)                                                                                                                |
| 025 | AI Agent Bridge — MCP Server for Ecosystem Introspection                                 | exeris-ai-bridge             | cross-repo           | public             | accepted (2026-05-15)                     | [exeris-ai-bridge/docs/adr/ADR-025 …](../exeris-ai-bridge/docs/adr/ADR-025-ai-agent-bridge.md)                                                                              |
| 026 | Client-Side Application API - CommunityWebClient                                         | exeris-kernel                | kernel/client-api    | public             | superseded (2026-05-19 — see ADR-034)     | [exeris-kernel/docs/adr/ADR-026 …](../exeris-kernel/docs/adr/ADR-026-client-side-application-api.md)                                                                        |
| 027 | Spring `ApplicationEventPublisher` / Exeris `EventBus` Bus Separation                    | exeris-spring-runtime        | spring               | public             | accepted (2026-05-17)                     | [exeris-spring-runtime/docs/adr/ADR-027 …](../exeris-spring-runtime/docs/adr/ADR-027-eventbus-applicationeventpublisher-boundary.md)                                        |
| 028 | Spring Boot 4 Nominal Compatibility Scope for 1.0                                        | exeris-spring-runtime        | spring               | public             | accepted (2026-05-17)                     | [exeris-spring-runtime/docs/adr/ADR-028 …](../exeris-spring-runtime/docs/adr/ADR-028-spring-boot-4-nominal-compatibility-scope.md)                                          |
| 029 | Phase 3B-α Scope — Request Scope and Structured Concurrency Helpers (kernel-independent) | exeris-spring-runtime        | spring/lifecycle     | public             | accepted, partially withdrawn (accepted 2026-05-17; **obligations 2 and 6 withdrawn 2026-08-08** per RFC-2026-08-08 and `exeris-spring-runtime#62` — a raw `StructuredTaskScope` was measured to propagate the `ScopedValue` binding by itself, so the `ExerisStructuredScope` rebind was redundant and obligation 6's tenant-isolation invariant belonged to the JDK rather than to this runtime. Obligations 1, 3, 4, 5 and 7 stand unchanged; `ExerisRequestScope` is unaffected and is now the whole of Phase 3B-α. Status shape mirrors the ADR file's own header) | [exeris-spring-runtime/docs/adr/ADR-029 …](../exeris-spring-runtime/docs/adr/ADR-029-phase-3b-alpha-scope-request-scope-and-structured-concurrency.md)                      |
| 030 | Phase 4C Scope — Spring-Side Seam for Kernel Graph SPI                                   | exeris-spring-runtime        | spring/graph         | public             | accepted (2026-05-17)                     | [exeris-spring-runtime/docs/adr/ADR-030 …](../exeris-spring-runtime/docs/adr/ADR-030-phase-4c-spring-side-seam-for-kernel-graph-spi.md)                                     |
| 031 | Phase 3B-β/γ Scope — Context Propagation + OTel Bridge (kernel-gated)                    | exeris-spring-runtime        | spring/observability | public             | reserved (2026-05-17 — content TBD)       | _(reserved; content pending in `exeris-spring-runtime/docs/adr/ADR-031-…`; kernel-gated on a **future** kernel tracing milestone (`TraceContext` carrier + OTLP sink), **not** 0.8.0 — see reservation note)_ |
| 032 | `HttpClientRequestEnricher` SPI — Implicit Context Propagation to Outbound HTTP          | exeris-kernel                | kernel/transport     | public             | accepted (2026-05-17)                     | [exeris-kernel/docs/adr/ADR-032 …](../exeris-kernel/docs/adr/ADR-032-http-client-request-enricher-spi.md)                                                                   |
| 033 | `KernelDiagnostics` SPI — Read-Only Runtime Introspection for Agent / CLI Adapters       | exeris-kernel                | cross-repo           | public             | accepted (2026-05-18)                     | [exeris-kernel/docs/adr/ADR-033 …](../exeris-kernel/docs/adr/ADR-033-kernel-diagnostics-spi.md)                                                                             |
| 034 | Client-Side Body Codec SPI — `HttpRequestBodyEncoder` / `HttpResponseBodyDecoder` + `KernelWebClient` facade | exeris-kernel | kernel/transport     | public             | accepted (2026-05-19)                     | [exeris-kernel/docs/adr/ADR-034 …](../exeris-kernel/docs/adr/ADR-034-client-side-body-codec-spi.md)                                                                         |
| 035 | Persistence Admission Control Tunability & Small-Pool Recalibration                      | exeris-kernel                | kernel/persistence   | public             | accepted (2026-05-29)                     | [exeris-kernel/docs/adr/ADR-035 …](../exeris-kernel/docs/adr/ADR-035-persistence-admission-control-tunability.md)                                                            |
| 036 | Server-Side Request Body Decoder SPI — `HttpRequestBodyDecoder` (completes the request/response × encode/decode codec matrix) | exeris-kernel | kernel/transport     | public             | accepted (2026-06-03)                     | [exeris-kernel/docs/adr/ADR-036 …](../exeris-kernel/docs/adr/ADR-036-server-side-request-body-decoder-spi.md)                                                                |
| 037 | Isolate the source-model parser/writer in a dedicated `exeris-sdk-source-model-io` module | exeris-sdk | cross-repo           | public             | accepted (2026-06-03)                     | [exeris-sdk/docs/adr/ADR-037 …](../exeris-sdk/docs/adr/ADR-037-source-model-io-module.md)                                                                                    |
| 038 | SDK realization of the capability annotation surface — `@CapabilityModule` / `@Provides` / `@Requires` / `@CapabilityLifecycle` (implements ADR-024) | exeris-sdk | cross-repo           | public             | accepted (2026-06-05)                     | [exeris-sdk/docs/adr/ADR-038 …](../exeris-sdk/docs/adr/ADR-038-capability-annotation-surface.md)                                                                            |
| 039 | Open-Core Observability Boundary — Shared Telemetry Wire Contract & Crash-File Decoder Cut | exeris-kernel | kernel/telemetry     | public             | accepted (2026-06-07)                     | [exeris-kernel/docs/adr/ADR-039 …](../exeris-kernel/docs/adr/ADR-039-open-core-observability-boundary.md)                                                                    |
| 040 | Identity Provider SPI — Pluggable Token Validation + `SecurityProvider` Dispatcher          | exeris-kernel | kernel/security      | public             | accepted (2026-06-24 — driven by RFC-2026-06-08, ACCEPTED; impl in v0.10) | [exeris-kernel/docs/adr/ADR-040 …](../exeris-kernel/docs/adr/ADR-040-identity-provider-spi.md) (content on the kernel v0.10 train; reaches `main` with the 0.10 release) |
| 041 | Compatibility-Mode Resource-Server Security Under `web-application-type=none` — Re-activate the Web-Gated JWT Decoder | exeris-spring-runtime | per-repo | public | accepted (2026-06-09) | [exeris-spring-runtime/docs/adr/ADR-041 …](../exeris-spring-runtime/docs/adr/ADR-041-compat-resource-server-security-under-none.md) |
| 042 | 0.5.0 bidirectional mutation surface — `MutationOp` / `MutationResult` + AST-level three-way conflict resolution & baseline-trust metadata (implements RFC-2026-06-11) | exeris-sdk | cross-repo | public | accepted (2026-06-13 — driven by RFC-2026-06-11, ACCEPTED) | [exeris-sdk/docs/adr/ADR-042 …](../exeris-sdk/docs/adr/ADR-042-bidirectional-mutation-surface.md) |
| 043 | Kernel HTTP Streaming SPI — server-push (SSE-first; sibling `HttpStreamExchange`) | exeris-kernel | kernel/transport | public | accepted (2026-06-21 — ratifies RFC-2026-06-18 Option 1; impl in v0.10) | [exeris-kernel/docs/adr/ADR-043 …](../exeris-kernel/docs/adr/ADR-043-kernel-http-streaming-spi.md) (driven by [RFC-2026-06-18](../exeris-kernel/docs/rfc/RFC-2026-06-18-http-streaming-spi.md), ACCEPTED; WebSocket deferred as a separately-justified follow-up) |
| 044 | Tooling SSE Stream Emitter Shape — entity-level live-view + per-action drivers; named-event SSE (`addEventListener`) client; domain-event-bus producer seam (consumes ADR-043) | exeris-tooling | tooling/codegen | public | accepted (2026-06-24 — ratifies RFC-2026-06-22; consumes ADR-043) | [exeris-tooling/docs/adr/ADR-044 …](../exeris-tooling/docs/adr/ADR-044-tooling-sse-stream-emitter-shape.md) (Slice 1 shipped in PR #104; per-action Slice 2 shape ratified, impl pending) |
| 045 | `HttpRetryPolicy` SPI — opt-in client-side retry for `KernelWebClient` (resolves the retry deferral from ADR-026 §"no implicit retry" / ADR-032 Alternative D) | exeris-kernel | kernel/transport | public | accepted (2026-06-25 — behavioural defaults in `CommunityHttpRetryPolicy`, SPI surface only in the ADR; implemented on the kernel v0.10 train, Sprint 4 (`exeris-kernel` **#220**), reached `main` with the 0.10.0 release integration and is tagged in v0.10.0) | [exeris-kernel/docs/adr/ADR-045 …](../exeris-kernel/docs/adr/ADR-045-client-side-http-retry-policy-spi.md) |
| 046 | Event-Payload Codec SPI — `EventPayloadCodec` + `EventPayloadCodecRegistry`; pluggable domain-event payload serialization (mirrors the body-codec matrix ADR-009/034/036; unblocks EV1 generated-event payloads) | exeris-kernel | kernel/events | public | reserved (2026-06-29 — targeted v0.10, paired with the Events `topic` item; content DRAFT in `exeris-kernel/docs/adr/`; kernel SPI + Community driver + TCK landing, generator + bootstrap binding lockstep) | [exeris-kernel/docs/adr/ADR-046 …](../exeris-kernel/docs/adr/ADR-046-event-payload-codec-spi.md) (DRAFT — flips to accepted when the lockstep consumer lands) |
| 047 | Consume the `@View` leaf field facet end-to-end — subsuming and removing `@UI` (processor populates `ComponentNodeMetadata.field`; codegen-ts `ViewGenerator` emits the field control; the leaf facet is `ViewFieldMetadata`, the renamed/expanded successor of the seed's `UIFieldMetadata`) | exeris-tooling | tooling/codegen | public | accepted (2026-06-29 — driven by RFC-2026-06-28; founder decisions baked: unify on `@View`, `UIFieldMetadata`→`ViewFieldMetadata`, entity-driven expansion is the model, no dual-path, `@UI` removed) | [exeris-tooling/docs/adr/ADR-047 …](../exeris-tooling/docs/adr/ADR-047-view-leaf-field-facet-and-ui-subsumption.md) |
| 048 | Cross-app contract mesh — generated cross-application contract registry + remote client + saga remote-dispatch (T12; capability twin T17) | exeris-tooling | tooling/codegen | public | reserved (2026-06-29 — target 0.7.0; content TBD, RFC-gated; ADR-triggering — changes pipeline shape and introduces a new cross-app contract surface) | _(reserved; content pending in `exeris-tooling/docs/adr/ADR-048-…`; codegen-core today carries only the closed-world `CapabilityGraph` — no cross-app contract registry, remote client, or saga remote-dispatch; the largest remaining generation gap for the dog-food app, hand-written today as a mesh + remote-client + park/await stand-in)_ |
| 049 | Events Log-Ordering & Optimistic-Concurrency Boundary — `EventStreamAppender` (Events SPI) owns append-with-expected-version + per-`StreamId` total ordering on the durable log; transient `EventBus` unordered by design; `FlowSnapshot` CAS stays Persistence-owned (ADR-013) | exeris-kernel | kernel/events | public | accepted (2026-07-01 — founder-ruled decision-only slice on the kernel v0.10 train; ADR + `events.md` boundary record + `EventStreamAppender` Javadoc; the SPI signature change (`expectedVersion` + `AppendResult`), `EX-EVENT-6008` (version conflict), and `AbstractEventStreamAppenderTck` / `ReaderTck` binding on ≥2 durable bindings are the deferred implementation slice, still v0.10) | [exeris-kernel/docs/adr/ADR-049 …](../exeris-kernel/docs/adr/ADR-049-events-log-ordering-and-optimistic-concurrency-boundary.md) (content on the kernel v0.10 train; reaches `main` with the 0.10 release) |
| 050 | Events Binding-Agnostic `topic` — the SDK `@DomainEvent.topic` gains a kernel sink as an optional `String topic` on `EventTypeSpec` (per-event-**type** registration carrier, NOT a per-instance `EventDescriptor` field); the Kafka binding honours it as the routing topic on both publish and subscribe (override of the default type-name topic); the in-memory `EventBus` treats it as advisory (topic-blind by design, ADR-049 H2) | exeris-kernel | kernel/events | public | accepted (2026-07-01 — founder-ruled; unblocks the `exeris-tooling` `KernelEventGenerator` which already reads `@DomainEvent.topic` but had nowhere to land it, so it was dropped to a Javadoc-only reference. Deviates from the ROADMAP's loose "field on the descriptor" phrasing: topic is a per-type static attribute so it rides `EventTypeSpec` alongside the existing `name` String (off the hot dispatch path) — the `EventDescriptor` primitive-only Valhalla record and both wire codecs stay byte-for-byte unchanged. Lockstep: tooling generator populate + e2e, SDK annotation Javadoc stance) | [exeris-kernel/docs/adr/ADR-050 …](../exeris-kernel/docs/adr/ADR-050-events-binding-agnostic-topic.md) (content on the kernel v0.10 train; reaches `main` with the 0.10 release) |
| 051 | PAQS Execution-Seam (`StreamExecutionBackend`) — agnostic Core-internal stream-execution injection point in `PaqsScheduler`; default = one Virtual Thread per stream (behaviour-neutral, the prior inline spawn); pluggable across {default / locality-affine / deterministic-simulation} backends; Enterprise supplies its own io_uring-affine backend out-of-repo; kernel-wide promotion (the other VT-spawn sites) gated on the DST RFC | exeris-kernel | kernel/transport | public | reserved (2026-07-04 — target 0.11.0; ports the refactor-neutral M1 seam from `research/loom-continuation-locality` onto the default line as the v0.11 first move; records the locality NO_GO as **config-scoped** (Community-only, stock VT scheduler with the custom-scheduler override reflectively absent, no `pullerMode=3`; Enterprise io_uring M4/M5 never ran), not a refutation; content authored, held-for-push on `feat/v011-paqs-execution-seam-port`) | [exeris-kernel/docs/adr/ADR-051 …](../exeris-kernel/docs/adr/ADR-051-paqs-execution-seam.md) (content on the kernel v0.11 train) |
| 052 | Community JSON Mapper Customization Seam — `JsonMapperCustomizer` / `JsonMapperScope`: a per-quadrant `ServiceLoader` hook in `exeris-kernel-community` that customizes the `tools.jackson` `ObjectMapper` feeding the already-mapper-injectable body/event codec drivers (ADR-009/034/036/046); Community-internal, additive, default byte-identical, Blackbird app-supplied | exeris-kernel | kernel/community | public | reserved (2026-07-19 — target v0.10.1; per-repo, **no** `exeris-tooling`/enterprise lockstep — the seam is upstream of registry construction in `CommunityHttpProvider` / `CommunityEventProvider`, so generated code is unaffected; content on kernel PR exeris-systems/exeris-kernel#239, accepted-on-implementation. Originally drafted as ADR-051; renumbered after ADR-051 was concurrently reserved for the PAQS execution-seam (#33)) | [exeris-kernel/docs/adr/ADR-052 …](../exeris-kernel/docs/adr/ADR-052-community-json-mapper-customization-seam.md) (content on the kernel v0.10.1 train) |
| 053 | SKU Composition Manifest Format — JSON (canonical `composition.json`; schema owned by `exeris-sdk-composition-spec`, canonical reader by the `exeris-tooling` pipeline; resolves the *format* half of ADR-024 open follow-up 3 — signing detail stays delegated to the `exeris-sku-*` repository convention) | exeris-docs | platform | public | accepted (2026-07-21 — founder-ruled alongside the gateway-caps implementation-plan adoption; first enforced artefact ships with the `exeris-sku-api-gateway` scaffold) | [adr/ADR-053 …](adr/ADR-053-sku-composition-manifest-format.md) |
| 054 | Field/Validation constraint-scoping cut — `@Validation` is the sole declaration site of `min`/`max`/`minLength`/`maxLength`/`pattern` and `FieldMetadata` the sole AST carrier (populated from `@Validation`); `ValidationMetadata` removed outright in 0.9.0 (never-consumed orphan, zero published artifacts — no deprecation window needed); per-component `NON_NULL` boxed-zero fix (`SchemaVersion` → `"0.9.0"`) + `-io` reader `minLength`/`maxLength` parity restored — resolves the 0.2.0-deferred overlap on budgetHQ corpus evidence (19/19 usages on `@Validation`) | exeris-sdk | cross-repo | public | accepted (2026-07-21 — evidence-driven close of the 0.6–0.9 deferral; tooling lockstep: SDK pin bump, dead TS `ValidationMetadataSchema` removal, one-time codegen re-run on the schema skew) | [exeris-sdk/docs/adr/ADR-054 …](../exeris-sdk/docs/adr/ADR-054-field-validation-min-max-pattern-cut.md) (content **merged to exeris-sdk `main`** via [exeris-systems/exeris-sdk#82](https://github.com/exeris-systems/exeris-sdk/pull/82), 2026-07-22 — the ADR's Decision and obligation 3 record the same "removed outright, no deprecation window" ruling as this row, including the within-release upgrade from the initial deprecate-then-remove draft) |
| 055 | Cap-tier Wall guard — build-time **bytecode** boundary enforcement in the tooling pipeline: the tooling-side realization of ADR-024 validation predicate 4 (as ADR-038 is its SDK-side realization). A JDK-standard `java.lang.classfile` scan over the compiled cap classes, wired into `exeris:verify-capabilities` at `process-classes`, rejecting imports of host-runtime packages (`org.springframework.*`, `io.netty.*`, `reactor.*`, `jakarta.servlet.*`), kernel internals (`eu.exeris.kernel.**.internal.**`), and *sibling*-cap internals (`eu.exeris.caps.<other>.internal.**`; a cap's own internals stay legal) | exeris-tooling | tooling | public | accepted (2026-07-30 — tooling 0.7.0 slice G1, the gateway-caps first-SKU plan item P1.3; content landed with the implementation PR `exeris-tooling` **#143** (merged 2026-07-30), accepted-on-merge per the ADR-047 per-repo pattern) | [exeris-tooling/docs/adr/ADR-055 …](../exeris-tooling/docs/adr/ADR-055-cap-tier-wall-guard.md) |
| 056 | `BlobStorageProvider` SPI — binary-object storage as a kernel seam: `BlobStorageProvider` + `BlobRef` + `BlobUploadHandle` / `BlobDownloadHandle` in a **net-new top-level SPI package** `eu.exeris.kernel.spi.storage.blob` (the 17th, alongside the existing 16). Streaming I/O on `LoanedBuffer` / `MemorySegment` with an explicit ownership rule across the upload and download halves — no `byte[]` round-trip on the transfer path, which is what makes this a kernel seam rather than a thin wrapper over a vendor SDK. Rules two contract boundaries the SPI must state rather than imply: what a **signed URL** promises at SPI level versus what stays driver-defined, and how `StorageContext.isolationKey` (ADR-012) scopes objects into tenant-segregated buckets or prefixes — composing with, but not depending on, the §4b `sharedScopeKey` row-visibility axis | exeris-kernel | kernel/storage | public | reserved (2026-07-30 — target 0.11.0, Tier-1 stream B; two in-repo bindings planned to close the ≥2-binding gate: a local-FS driver and a minimal S3-compatible driver over `KernelWebClient` scoped to single-object PUT / GET / ranged-GET + presigned-URL generation, MinIO path-style, no multipart and no full SigV4 grid — the auth subset is documented in the ADR rather than left to driver folklore. Content lands with the docs-only PR-A slice, ahead of the SPI) | [exeris-kernel/docs/adr/ADR-056 …](../exeris-kernel/docs/adr/ADR-056-blob-storage-provider-spi.md) (content on the kernel v0.11 train) |
| 057 | `JobScheduler` SPI — background-job execution as a kernel seam: `JobSchedulerProvider` (discovery) + `JobScheduler` / `JobDescriptor` / `JobHandle` / `JobTrigger` (cron / interval / one-shot) in a net-new SPI package `eu.exeris.kernel.spi.scheduling`, with `PrincipalContext` + `StorageContext` captured at submission and rebound via `ScopedValue` on dispatch, so a job runs under the identity that scheduled it instead of unauthenticated. Two rulings deviate from the ROADMAP entry's sketch and are the ADR's substance: (a) the Community driver dispatches on **virtual threads + explicit `ScopedValue` rebind** rather than `StructuredTaskScope`, which would open a *fifth* preview-taint site on the default line (four exist: `CommunityEventLoop`, `SubsystemOrchestrator`, `OutboxOrchestrator`, `InMemoryEventBus`) against the Platform-Baseline preview-clean mandate for 1.0 GA; (b) **no `ScheduledExecutorService` and therefore no scoped-ban exception** — the ROADMAP asks for both an injectable time source and `ScheduledThreadPoolExecutor`, and those are mutually exclusive, since STPE times off `System.nanoTime()` internally and cannot be driven by an injected clock, so a deterministic trigger TCK is unreachable through it | exeris-kernel | kernel/scheduling | public | reserved (2026-07-30 — target 0.11.0, Tier-1 stream C; leader election and durable-queue semantics are explicitly **out** of v0.11, named as the first consumer of the cross-node coordination seam rather than a parallel mechanism. Content lands with the docs-only PR-A slice, ahead of the SPI) | [exeris-kernel/docs/adr/ADR-057 …](../exeris-kernel/docs/adr/ADR-057-job-scheduler-spi.md) (content on the kernel v0.11 train) |
| 058 | Generated tests: emission channel + dependency contract — the tooling pipeline gains a **second output root** (`src/test/generated/java`, its own `OutputWriter` and T13 manifest, registered via `addTestCompileSourceRoot`) behind an opt-in `exeris.tests` flag, and fixes what a generated test may import at **JUnit 5 + AssertJ and nothing else** — doubles are emitted (an `HttpExchange` recorder; a service stub by subclassing the generated service) rather than mocked, because tooling emits no `pom.xml`, so every import is a hard requirement on the consumer's build | exeris-tooling | tooling | public | accepted (2026-07-31 — target tooling 0.7.0, backlog item T2 slice a; content landed with the implementation PR `exeris-tooling` **#147** (merged 2026-07-31), accepted-on-merge per the ADR-047/ADR-055 per-repo pattern) | [exeris-tooling/docs/adr/ADR-058 …](../exeris-tooling/docs/adr/ADR-058-generated-test-emission-channel.md) |
| 059 | Data-scope expression — `@ExerisDomain.dataScope` with a mutually-exclusive `DataScope { GLOBAL, TENANT, UNIVERSE }` discriminator supersedes the `tenantScoped` boolean, which is deprecated for removal at 1.0.0 (fallback `true→TENANT` / `false→GLOBAL`). The enum is **duplicated, not shared** — an annotation-side type (with an extra `UNSPECIFIED`, since annotation attributes cannot default to `null`) and an independent AST-owned `eu.exeris.sdk.sourcemodel.ast.DataScope` mapped by name at extraction, per the `SagaStep.StepKind` precedent, preserving the annotations module's zero-dependency contract. The fallback lives in the AST (`DomainMetadata.effectiveDataScope()`, never null), so pre-0.10.0 baselines read back unchanged. `UNIVERSE` ships **reserved**: the kernel enforces the tier (ADR-012 §4b amendment on the 0.11 line — `sharedScopeKey` carrier + shared-scope claim + `exeris.shared_scope` read-widen/write-pin RLS + `AbstractSharedScopeAccessMatrixTck`), but the `exeris-tooling` transcription mapping the tier onto that carrier is not built, so declaring it has no generated effect yet and the javadoc says so. `SchemaVersion` → `"0.10.0"`; `-io` reader parity deliberately deferred to land lockstep with the processor (ADR-042 discipline) | exeris-sdk | cross-repo | public | accepted (2026-07-31 — the build-start ADR RFC-2026-06-24 reserved for trigger (2); target SDK 0.10.0. Content landed with the implementation PR `exeris-sdk` **#89** (merged 2026-08-02), accepted-on-merge per the ADR-047/ADR-058 per-repo pattern. Sequencing note: deprecation and removal cannot share a release and 1.x is additive-only, so `tenantScoped` must be deprecated in a 0.x minor or be frozen through the whole 1.x line — that constraint is why 0.10.0 exists as a milestone) | [exeris-sdk/docs/adr/ADR-059 …](../exeris-sdk/docs/adr/ADR-059-data-scope-expression.md) |
| 060 | Generated code logs through `System.Logger` — the Java emitters bind **no logging facade**. Eight of them put an `org.slf4j.Logger` into generated **main** code, which made `slf4j-api` a hard requirement on every consumer's build (tooling emits no `pom.xml`) — and it is a dependency of neither `exeris-kernel-spi` nor `-core`, so it reached an application only through the driver tier (`exeris-kernel-community` pulls it): a consumer on a different driver set could have generated code that does not compile, against a requirement no document carried. `java.lang.System.Logger` ships with every JDK, so the generated tree adds no logging dependency at all, and routing moves to the application via a `System.LoggerFinder` provider. The same question ADR-058 answered for the generated *test* surface, asked of the main one — and found by the same instrument: naming the compile classpath instead of inheriting `java.class.path`. Three API differences are recorded with the decision because they are silent-failure shaped: `{}` → `{0}` (`MessageFormat`), single quotes in a parameterised message must be doubled (a placeholder inside quotes is emitted verbatim and its argument dropped), and there is no trailing-`Throwable` convention | exeris-tooling | tooling | public | accepted (2026-08-01 — tooling 0.7.0; content landed with the implementation PR, accepted-on-merge per the ADR-047/ADR-055/ADR-058 per-repo pattern) | [exeris-tooling/docs/adr/ADR-060 …](../exeris-tooling/docs/adr/ADR-060-generated-code-logging-facade.md) |
| 061 | Declarable HTTP route-authorization policy — the kernel's route-admission decision moves from a hardcoded path convention to a contract the application declares. Today `CommunityHttpRequestDispatcher` gates on `path.startsWith("/secure")` with literal `security:read` / `security:write` scopes compiled into the driver, so an application cannot state which of *its own* routes need a principal; everything outside that prefix reaches the handler with no `PrincipalContext` bound at all. Three placements are ruled: the policy contract in `eu.exeris.kernel.spi.http` behind an `Optional` slot on `HttpKernelProviders` (the ADR-036 `HttpRequestBodyDecoderRegistry` shape, not a new `KernelProviders` slot); the driver-agnostic decision helper in `eu.exeris.kernel.core.security` alongside `RoleCheckEnforcer`, so every transport inherits one decision layer instead of each driver growing its own; and the rules themselves **declared in code, never in a configuration file** — ADR-014 §3 rejected config-file-driven role policy for drift surface, and this ADR honours that rather than quietly reversing it. Two consequences ship with it: a Community **security `Subsystem`**, whose absence is code-vs-contract drift (`bootstrap.md` already places Security in the L1 parallel-init DAG, and `CommunitySecurityProvider` is `ServiceLoader`-registered, but nothing binds `KernelProviders.SECURITY_PROVIDER` — so in a default boot the whole Citadel path (interceptor, role mask, `StorageContextBridge`) is unreachable and `/secure/*` answers 401 unconditionally); and removal of the dispatcher's `isPublicPath` allowlist, which is unreachable code — it is only consulted behind `startsWith("/secure")`, so no path can satisfy both conditions — left behind when the health routes moved to `CommunityHttpHealthRoutes`. Explicitly **does not** revise: the Sprint-4 descoping of kernel-edge `methodId` enforcement, since `methodId` is assigned at compile time from alphabetical ordering under `@Retention(SOURCE)` and the kernel therefore cannot reconstruct a URL→`methodId` map at runtime *at all* — which makes the path-based mechanism the only one the kernel can execute unaided, not a stand-in for absent tooling; ADR-014 §3; or ADR-012 admission semantics (missing/invalid token → 401, authenticated without the required scope → 403). Default is "no policy declared", so an application that declares nothing behaves exactly as it does today | exeris-kernel | kernel/security | public | accepted (2026-08-05 — kernel 0.11.0; merge gate met: `AbstractHttpRoutePolicyTck` with `CommunityHttpRoutePolicyTckTest` binding it. A later `exeris-spring-runtime` DSL translating Spring-shaped rules onto this decision layer is deliberately **out of scope** and gets its own ADR — this one exists so that binding is a thin translation rather than a second authorization mechanism standing beside the first) | [exeris-kernel/docs/adr/ADR-061 …](../exeris-kernel/docs/adr/ADR-061-declarable-http-route-authorization-policy.md) (content on the kernel v0.11 train) |
| 062 | Flow resume binds to a named step, not a position — the minimal correctness core of the definition-versioning epic. A parked saga persists `FlowSnapshot.currentStep`, a zero-based `int`, and resume replays it into whatever plan is registered under the definition name. v0.10 added `CoreFlowRuntime.validateSnapshotStepBounds` (`:504`), but it tests `step < 0 \|\| step >= stepCount` and therefore cannot see a **same-arity reorder**: the index stays in range and the saga silently resumes on a *different* step — a data-corruption class, not a missing feature, and the scenario `docs/subsystems/flow.md:37` already names as highest-risk while deferring it to this epic. The decision: `FlowStepDescriptor.name` is promoted from its current "human-readable, for JFR / diagnostics" status to a **step identity** — unique within a definition, validated when the definition is built rather than trusted — and `FlowSnapshot` carries the resumed step's identity alongside `currentStep` (additive; pre-1.0, no external SPI consumers). Resume then validates identity, not merely arity, and a reorder fails closed through the existing `EX-FLOW-7002 / phase=SCHEMA_MISMATCH` surface rather than a new error taxonomy. **`stepId` is deliberately not repurposed**: it is the slab slot address (`address = stepSlabBase + stepId * STEP_DESCRIPTOR_STRIDE`), so turning it into a stable identifier would cost the Enterprise tier its O(1) descriptor lookup — position and identity are two different things and this ADR keeps them that way. Also the prerequisite for the FlowJournal work: a journal recording step *positions* would be a history that becomes false at the next deploy, so "STEP_COMPLETED step=3" means nothing once a step is inserted ahead of it | exeris-kernel | kernel/flow | public | accepted (2026-08-05 — kernel 0.11.0; merge gate met: `AbstractSagaRecoveryTck` covers both the same-arity reorder (`STEP_IDENTITY_MISMATCH`) and the pre-0.11 snapshot with no recorded identity (`STEP_IDENTITY_ABSENT`). Explicitly **out of scope**, and deliberately so: a `FlowDefinition` version field, coexisting definition versions, and in-flight migration — the "Camunda wedge" tracked in the kernel ROADMAP — because building version coexistence before the runtime can even *detect* a mismatch is building on sand. Those follow once identity lands. The ADR also corrects a stale roadmap claim that no step-bounds validation exists in code; it has since v0.10, and the audit note predates it) | [exeris-kernel/docs/adr/ADR-062 …](../exeris-kernel/docs/adr/ADR-062-flow-step-identity-on-resume.md) (content on the kernel v0.11 train) |
| 063 | `ExerisHttpSecurity` — Spring-shaped route rules compile onto the kernel's route-authorization decision, they do not re-implement it. Kernel ADR-061 placed `RouteAuthorizationEnforcer` in Core rather than in the Community driver specifically so a Spring DSL could translate onto it, and made that DSL out of scope pending this ADR. An application declares rules in a `ExerisHttpSecurity` bean; `exeris-spring-runtime-web` compiles them **once at startup** into a single `HttpRoutePolicy` bound into `HttpKernelProviders.HTTP_ROUTE_POLICY`. Enforcement stays entirely kernel-side, on the admission path, before `ExerisHttpDispatcher`/`ExerisCompatDispatcher` are reached — so a Spring-hosted deployment and a kernel-direct SKU share one decision layer instead of two that can disagree. Compile-once is a contract requirement, not an optimisation: `requirementFor` runs on every request and ADR-061 forbids per-request allocation there. Three constraints are ruled rather than discovered later: (1) `hasRole(...)` is **not** offered at the edge — `RouteRequirement` has no role kind and `RouteAuthorizationEnforcer` decides only via `PrincipalContext.hasScope`, so the DSL exposes scope-shaped predicates and roles stay with `@PreAuthorize`, which reads Spring authorities; inventing a `ROLE_x`→scope convention would put a second, silently-diverging authority model at the edge. (2) The unmatched route takes **no default** — the DSL requires an explicit answer, because `HttpRoutePolicy.unmatched()` is fail-closed and the kernel does not exempt the driver's own `/health`, `/health/live`, `/health/ready`, `/db/ping`, `/db/roundtrip`; Spring's idiomatic `anyRequest().authenticated()` maps straight onto fail-closed and would deny orchestrator probes on a healthy process, so declaring fail-closed without declaring the probe routes fails startup. (3) Authority parity is **deferred, not claimed**: `ClaimsMapper` is the documented application-customisable mapping point, but `CommunityOidcIdentityProvider` hardcodes `new CommunityClaimsMapper()`, so a Keycloak `realm_access.roles` token yields kernel scopes that differ from the Spring authorities the same token produces through an application `JwtAuthenticationConverter` — the ADR records the divergence and its blast radius rather than papering over it, and the fix is a kernel slice. Mode-neutral: the policy governs ingress for Pure and Compatibility Mode alike, so it is not a compat feature | exeris-spring-runtime | spring/security | public | accepted (2026-08-05 — single decider, decision made at draft time, ratified on merge of the PR introducing the file; content is on `main`. Required by kernel ADR-061 Engineering Protocol §5, which gates any `exeris-spring-runtime` binding on this ADR existing. The **decision** is settled; its **implementation** is still blocked upstream — the kernel pin cannot move until a 0.11.0 is released, and end-to-end verification needs the application-supplied `ClaimsMapper` that landed as exeris-kernel#287. Row said `reserved` until 2026-08-08, which understated a decision already made and contradicted the file's own `ACCEPTED` header) | [exeris-spring-runtime/docs/adr/ADR-063 …](../exeris-spring-runtime/docs/adr/ADR-063-exeris-http-security-route-policy-binding.md) |
| 064 | Flow definitions are versioned, and a parked saga resumes on the version it parked under — stage 2 of the definition-versioning epic, the "Camunda wedge". ADR-062 made resume detect a mismatch (step identity, fail-closed); it deliberately left version *coexistence* out, because building coexistence before the runtime can detect a mismatch is building on sand. This decides the coexistence half. `FlowDefinition` is keyed solely by `String name` today (`exeris-kernel-spi/.../flow/model/FlowDefinition.java:34`), and `CoreFlowRuntime.resolvePlanForSnapshot` (`:468`) rebinds a parked saga to whatever plan currently holds that name — so a saga that outlives a deploy resumes against a definition it never started under, which is exactly the scenario an orchestration engine is bought to survive. `FlowSnapshot.schemaVersion` is **not** this: it is the ADR-013 optimistic-lock counter on the durable row, a different axis entirely, and conflating the two would make every concurrent write look like a redeploy. Scope: a version on `FlowDefinition` (name+version key), carried in `FlowSnapshot`; resume resolving the *exact* version the saga parked under rather than the latest registered; and an in-flight migration contract — an explicit, testable vN→vN+1 transform, with **no migration path meaning rejection or quarantine, never a best-effort resume**. `loadByDefinition()`, deferred since FLOW-101 pending precisely this key, is wired once it exists. Prerequisite for the FlowJournal work in turn: an entry recording which step completed is only durable history if it also records which *version* produced it, or the journal ages out at the next deploy for the same reason positions did | exeris-kernel | kernel/flow | public | accepted (2026-08-06 — kernel 0.11.0; merge gate met: `AbstractFlowDefinitionVersioningTck` covers version-keyed resume, vN→vN+1 in-flight migration and no-migration-path rejection, bound by `CommunityFlowDefinitionVersioningTckTest`. Landed in three PRs — exeris-kernel#289 coexistence, #291 in-flight migration, #296 compensation-stack step identity. Five amendments settled during implementation: A1 resume-restore path only, A2 the transform returns the step the saga PARKED at rather than the one it resumes on, A3 a successful migration is persisted, A4 the compensation stack is validated too (obligation 9 covered two of five `FlowMigrationState` components), A5 the stack carries step identities — and A5 corrects A4's failure-mode sentence, since an in-range entry addressing a different step throws nothing at all rather than aborting the rollback. Still **not** in scope: the FlowJournal contract, which follows this rather than preceding it) | [exeris-kernel/docs/adr/ADR-064 …](../exeris-kernel/docs/adr/ADR-064-flow-definition-versioning-and-in-flight-migration.md) (content on the kernel v0.11 train) |
| 065 | The SPI stability declaration is machine-enforced — `docs/stability-matrix.md` states which SPI surfaces are settled, and until now nothing checked it, so a contract could move on a surface declared `stable` and surface later as a consumer's upgrade problem rather than as a build failure. A CI gate (`tools/spi-api-diff/`, outside the reactor alongside `tools/jfr-reporter`) diffs the public SPI against the last released version with japicmp and **fails the build on a binary-incompatible change to a `stable` surface**; `preview` and `experimental` are reported but not gated, which is the declared semver policy executed rather than restated. Four rulings carry the decision: (1) the gate **compiles the SPI from git** rather than resolving published artifacts — possible only because The Wall constrains `exeris-kernel-spi` to `java.*` / `jdk.*`, so every revision in the project's history builds with a bare JDK; the consequence is a gate that needs no GitHub Packages credentials, runs on fork PRs, and can regenerate the entire release record from a clean clone, including releases published before it existed. (2) Severity follows the **declared maturity label**, so the matrix becomes the gate's configuration instead of prose sitting beside it; `tools/spi-api-diff/stability-surfaces.conf` mirrors it and must move in the same commit, and where the generated record and the table disagree the record wins. (3) An SPI package carrying **no** maturity label fails the build — classification is mandatory, not opt-in; the first run found `spi.scheduling` and `spi.storage.blob` shipping on the 0.11 line with ADRs, TCKs and Community bindings but no matrix row. (4) A gate failure is a **decision prompt, not an automatic revert**: restore compatibility, or demote the surface in the matrix and record the change — the matrix is allowed to be wrong, it is not allowed to be quietly wrong. Records two findings that motivated it: source-level and file-inventory diffing is **not** a sufficient instrument (it cannot see removed record components, removed constructors, or interface-method removals where the file survives — it reported one incompatible transition across 0.5.0→0.10.2 where bytecode comparison finds three), and `FlowSnapshot` gaining a component under ADR-062 is a binary-incompatible change to `spi.flow`, a `stable` surface, which the gate flags and which is therefore a decision owed at the v0.11 cut rather than a free consequence of being pre-1.0 | exeris-kernel | kernel/build | public | accepted (2026-08-06 — kernel 0.11.0, exeris-kernel#290; content and implementation shipped in one PR, accepted-on-merge per the ADR-047/ADR-055/ADR-058 per-repo pattern. The gate has since run in anger: it correctly failed exeris-kernel#290 itself on a stale branch tip that had removed the 0.10.0 `FlowSnapshot` canonical constructor, and passed once the overload restoring it was merged in. Generated companion artefacts land with it: `docs/release/spi-api-history.md` (per-transition record, 0.5.0→0.10.2), `docs/release/upgrade-0.5-to-0.10.md`, and reconstructed `docs/release/v0.6.0-release-notes.md` — v0.6.0 shipped without notes and predates `CHANGELOG.md`, while carrying the only pre-declaration removal of `stable`-surface SPI types. The matrix was first published in v0.9.0; no `stable` surface has taken a binary-incompatible change since) | [exeris-kernel/docs/adr/ADR-065 …](../exeris-kernel/docs/adr/ADR-065-spi-compatibility-gate.md) (content on the kernel v0.11 train) |
| 066 | The default distributable line is preview-clean on a GA LTS — JDK 25, no `--enable-preview`, and `StructuredTaskScope` moves to the `preview` branch rather than staying on the critical path. Two blockers were measured separately rather than assumed together, and they behave very differently. **The JDK-26 target costs nothing**: the full reactor plus 747 tests is green on JDK 25.0.3 at `--release 25`, so no JDK-26-only API is used anywhere and the `26` in the build was arbitrary. **The preview flag is the whole of the work**, and it is exactly four sites — `core/bootstrap/SubsystemOrchestrator`, `core/events/InMemoryEventBus`, `core/events/outbox/OutboxOrchestrator`, `community/events/CommunityEventLoop` — with `exeris-kernel-spi` compiling clean, which upgrades The-Wall-holds-here from a grep result to a compiler result. The substitution is a Core-owned `fork`/`join`/`cancel` layer over virtual threads (GA 21) with `ScopedValue` (GA 25) rebound **explicitly** in the fork helper, because automatic binding inheritance is the part fused to `StructuredTaskScope` and there is no API to snapshot a thread's live bindings generically; the existing `AbstractProviderBindingLifecycleTck` and `ScopedValueBindingTest` make a propagation regression a red test rather than a silent one. Records a preview leak that no signature audit could have found, because it is not in a signature: `StructuredTaskScope.open()` defaults to `Joiner.awaitAllSuccessfulOrThrow()`, so `join()` throws the **preview** type `StructuredTaskScope.FailedException` (unchecked, verified by running it) — every non-FOUNDATION bootstrap phase starts in parallel, so a mandatory subsystem failure surfaces to callers as a preview class, and `CoreFailurePolicyTckTest:113` already catches `BootstrapException \| FailedException` for exactly that reason. Two consequences ship with the substitution: the parallel path throws `BootstrapException` uniformly, which makes `startParallel`'s post-join failure-collection block — dead today, since `join()` throws before it is reached — live for the first time; and the preview alternative disappears from the TCK catch. Also two build-tooling constants that fail the build *before* the STS errors and so mislead anyone attempting this without measuring first: `@SupportedSourceVersion(SourceVersion.RELEASE_26)` in both `exeris-kernel-build-config` annotation processors, and `--release 26` hardcoded in two processor tests. The motivation is not only that a "1.0 GA" resting on a preview API is internally contradictory: `--enable-preview` is a whole-compilation and whole-JVM flag whose bytecode is major-pinned, so it propagates to every consumer's entire codebase — an LTS-only, no-preview deployment constraint therefore cannot be satisfied by an intermediate step, since a preview-compiled class at `--release 25` still loads only on JDK 25 and only with the flag. Explicitly **not** in scope: JEP 401 value classes, which are preview in JDK 28 and belong to the `preview` branch's own track | exeris-kernel | kernel/platform | public | reserved (2026-08-08 — target kernel 0.11.0. Merge gate: the default Core + Community reactor compiles and all TCK/CI gates pass on JDK 25 LTS with no `--enable-preview`; a CI scan asserts zero `minor_version 0xFFFF` in shipped `main` bytecode; binding-propagation TCK green. Test and TCK fixtures may keep the flag — they are not distributed) | _(content pending — `exeris-kernel/docs/adr/ADR-066-…`)_ |
| 067 | Binary neutrality of the single published artefact across the Spring Boot dual matrix — ADR-028 obligation 2 runs both matrix axes in full and blocks merge on either, which verifies **two builds of the source**; `deploy.yml` runs `mvn clean deploy` with no `-P`, so exactly **one** jar is published, built by the default `matrix-sb3` profile, and ADR-028 claims it also runs on SB4. Nothing checked that claim, and it does not follow from the matrix: a call site can compile on **both** lines and bind to a **different descriptor** on each, so both axes go green while the shipped binary fails at runtime. Found downstream, in a running Compatibility Mode application on Spring Boot 4, after the matrix had been green for the whole train — `ExerisResponseEntityReturnValueHandler` copies response headers with `putAll(entity.getHeaders())`, which binds `putAll(Map)` under SB3 and `putAll(HttpHeaders)` under SB4 because Spring Framework 7 stopped `HttpHeaders` implementing `MultiValueMap`; the SB3 binary therefore hands a non-`Map` to a `Map` parameter and **every** `@RestController` returning `ResponseEntity<T>` dies with `IncompatibleClassChangeError`. A controller returning a bare `List<T>` survives — it goes through `ExerisResponseBodyReturnValueHandler` and never reaches that line — which made a total failure of one handler look like a selective one. A sweep found a second, absent from the bug report: `getHeaders().get(name)` binds `get(Object)` (inherited from `Map`) under SB3 and `get(String)` under SB4 → `NoSuchMethodError` on any multi-valued `@RequestHeader`. **The decision:** keep one artefact serving both lines, and make binary neutrality an enforced obligation rather than an assumption — a CI job compiles the reactor under both profiles and diffs every constant-pool reference our `target/classes` make into `org/springframework/**`, failing the build and naming the call site on any delta (262 references today; 2 differed before the fix, 0 after; method references behind `invokedynamic` included). Both call sites move to members whose signature is identical on both lines — `forEach` + `put(String, List)`, and `forEach` with a case-insensitive name match — verified by compiling against `spring-web` 6.2.7 and executing on 7.0.8. Rejected: **per-line classifiers**, which double the published surface and the consumer's version decision without removing the need for the check, only moving it; and a **denylist of known-drifting members**, which tracks the last failure instead of the class of failure. Records the root cause as a documentation defect, not a coding one: "one source compiles under both profiles" was the closure criterion used across the whole SB4 friction inventory, and it is necessary and not sufficient — it catches anything that *stops compiling* and is blind to anything that keeps compiling against a changed signature, which is how `spring-boot-4-matrix.md` item 3 was closed while leaving two live defects behind it. Explicitly **not** in scope: the Spring Security 7 axis (ADR-028 obligation 6), and source-level divergence that fails to compile, which the existing matrix already covers | exeris-spring-runtime | spring/build | public | accepted (2026-08-08 — `exeris-spring-runtime` 0.7.0, the train the dual matrix itself landed in; content, the two fixes and the enforcing CI job ship in one PR, accepted-on-merge per the repo's single-decider pattern) | [exeris-spring-runtime/docs/adr/ADR-067 …](../exeris-spring-runtime/docs/adr/ADR-067-binary-neutrality-of-the-published-artefact.md) |
| 068 | `exeris-spring-runtime` follows the kernel's two-track JDK model with **two published artefacts** — a GA line and a `-preview` line — and the `-preview` line exists for **Valhalla**, not for `StructuredTaskScope`. Kernel ADR-066 splits the platform into a preview-clean LTS default line and a `preview` branch on the latest JDK; that branch converges into `main` at an LTS, so a component that does not track it absorbs the whole API delta at the LTS instead of ahead of it. The forcing content is JDK 28's JEP 401 (Value Objects, preview) with JEP 539 (Strict Field Initialization) as its soundness companion: value classes are what this runtime's carriers — `RequestScope` first — would actually be exercised against, and that exercise has to happen before the preview line *becomes* the GA line. Verified on `28-ea+10` rather than inferred from the release calendar: `value class` and `value record` compile and run under `--release 28 --enable-preview`, the emitted field carries `ACC_STRICT_INIT` (JEP 539 in effect), `RequestScope`'s exact shape — `value record RequestScope(UUID, String, Map<String,Object>)` — works, and `ScopedValue` stays GA with no flag. **Rules three things.** (1) **Two artefacts, not four**: the Spring Boot axis stays classifier-free per ADR-067, so each JDK-line artefact serves both SB3 and SB4; only the JDK axis takes a classifier, because one jar cannot be two class-file majors. (2) **The `-preview` artefact ships with content from its first release**, not as a JDK-target rebuild of the GA jar — a jar at `--release 25` (major 69) loads unchanged on a JDK 28 JVM, so a content-free `-preview` coordinate would be a second distribution consumers must reason about in exchange for nothing. (3) **A deliberate per-line source overlay is accepted, and bounded.** This is the ADR's real cost and it is stated rather than glossed: `value record` and `record` are different source, so ADR-028 obligation 1's "one source tree" cannot hold for the carriers. RFC-2026-08-08 had just removed the *accidental* split (`ExerisStructuredScope`, a no-op wrapper); this reintroduces a *deliberate* one, for an exercise rather than by neglect. The overlay is confined to carrier types, and a guard asserts each overlay class has public API identical to its GA counterpart, so the divergence is a modifier and never a contract | exeris-spring-runtime | spring/build | public | accepted (2026-08-08 — content merged in `exeris-spring-runtime#62`; single decider, decision made at draft time, ratified on merge. Reserved by #54 and promoted here, the two-step flow used for ADR-030 and ADR-033. **The architecture-guard layer does not run on the preview line** — ArchUnit 1.5.0 tops out at JDK 27, and the failure mode is the disqualifier rather than the missing version: an ASM that cannot read the class-file major *silently skips* those classes, so all 16 guard tests would report green having inspected nothing (this repo recorded exactly that behaviour at major 70 with ArchUnit 1.3.0). The exclusion is explicit, the GA line is the sole authority for The Wall and the module boundaries, and the overlay identity guard is reflection-based rather than ArchUnit-based so it survives the ceiling. **Prerequisite, measured rather than assumed: a preview-clean kernel.** Preview class files are major-pinned — a JDK 28 JVM reports `UnsupportedClassVersionError: class file version 70.65535 … only recognizes preview features for class file version 72.65535` — so kernel 0.10.2, itself compiled with preview on JDK 26, cannot load on the preview line at all. The `-preview` artefact therefore waits on the kernel's own ADR-066 substitution, not on JDK 28, which is downloadable today. Supplements ADR-067, whose rejection of per-line classifiers governs the Spring axis only and does not transfer: one jar can serve two Spring lines, it cannot serve two class-file majors. An earlier draft of this row claimed execution was gated on an unreleased JDK 28 — that was inferred from the GA calendar instead of checked, and 28-EA has been downloadable throughout) | [exeris-spring-runtime/docs/adr/ADR-068 …](../exeris-spring-runtime/docs/adr/ADR-068-two-track-jdk-artefact-model.md) |
| 069 | The SDK baselines on **JDK 25 LTS** and ships **one** artifact line. Kernel ADR-066 moved the distributable kernel line to 25 LTS with no `--enable-preview` and measured the JDK-26 target as costing nothing ("the `26` in the build was never load-bearing"), which falsified this repo's own stated reason for its floor — `README.md` said the SDK is pinned to 26 "because the **Exeris platform kernel** is". The gap is load-bearing here in a way it is nowhere else: `exeris-sdk-annotations` and `exeris-sdk-source-model` are **compile-classpath** artifacts for a consumer's own build, and `javac` on JDK 25 refuses a major-70 class outright ("class file has wrong version 70.0, should be 69.0") — so the LTS-only, no-preview consumer ADR-066 exists to admit was unblocked at the kernel and then stopped at the **most upstream repo in the ecosystem**, with every Exeris annotation unreachable. Taken before the 1.0.0 freeze rather than after: lowering a floor stays legal in a 1.x minor because it widens, but a GA freezing a surface an LTS consumer cannot put on a classpath is not a state worth shipping deliberately. **Measured rather than assumed**, the same probe ADR-066 ran: the reactor is green at `--release 25` across all six modules with every gate (JaCoCo 85%, both snapshot guards, javadoc), the emitted jars carry class-file major 69, and no main source uses API above the 21-era record / sealed set. The authoring machine has only a JRE 25, so that probe cross-compiled with javac 26 at `--release 25`; the decision therefore ships its own CI evidence — a JDK 25 row that compiled the reactor at `release 25` on Temurin 25 and passed. **Three rulings.** (1) The floor **follows** the kernel's GA baseline and never leads it — raising above it locks out LTS consumers, lowering below what the sources need buys nothing, and 21 would re-introduce the same mismatch pointing the other way. (2) **One line, no `-preview` artifact** — declining ADR-068's two-track model by ADR-068's own reasoning: that model exists for JEP 401 value classes, which are *different source* needing exercise before the preview line becomes GA, while the SDK declares annotations and records with no preview construct to adopt, and a jar at major 69 loads unchanged on a JDK 28 JVM, so an SDK `-preview` coordinate would be the content-free rebuild ADR-068 ruling (2) rejects. Recorded explicitly because with two repos now two-track, "the SDK follows the kernel" would otherwise read as tracking both lines — it follows the **baseline**, not the **bifurcation**. (3) A guard reads the **class-file major**, not the build property: `ClassFileBaselineTest` fails above 69, so a module-level `<release>` or a plugin default that re-raises the target cannot slip past an assertion on `maven.compiler.release` (verified non-vacuous by building at 26). Retires the repo's "JDK 26 is not negotiable" invariant across `CLAUDE.md`, `README.md`, `ROADMAP.md`, the annotations `package-info`, `DrainRunner`'s rationale and seven `.claude/` guardrail files. Explicitly **not** in scope: `exeris-tooling`'s own baseline — its processor runs inside a consumer's `javac`, so an LTS consumer needs that jar at major 69 too; this decision is the prerequisite for it, not a substitute | exeris-sdk | sdk/build | public | accepted (2026-08-12 — `exeris-sdk` 0.10.0, exeris-sdk#93; content, the build change and the guard ship in one PR, accepted-on-merge per the ADR-065/ADR-067 single-decider pattern. CI evidence at the PR: `mvn verify (JDK 25)` and `(JDK 26)` both green) | [exeris-sdk/docs/adr/ADR-069 …](../exeris-sdk/docs/adr/ADR-069-jdk-baseline-lts.md) |


> **Numbering note:** ADR-004 (2025-12-26) post-dates ADR-007's original acceptance (2025-12-11) by 15 days — conceptual ordering ("JDK target → runtime → telemetry → boundaries") is preserved over strict chronology. The 004–006 and 011 slots were reserved historically for early platform/cross-cutting decisions and are now filled.
>
> **Reservation note:** ADR-031 is a number reservation against future name collisions; content lands in a follow-up PR. Per Rule 2, the number is held by this index entry, not by the existence of a content file. ADR-031 covers the kernel-gated half of Phase 3B (W3C `traceparent` propagation + OTel span/metric emission) and waits on a **future** kernel tracing milestone — **not** delivered in `exeris-kernel` 0.8.0. As of 0.8.0 (shipped 2026-06-03) the kernel has **no** tracing logic at all: no `TraceContext` `ScopedValue` carrier, no `PrometheusOtlpTelemetrySink`, no span emission in SPI/Core/Community (the `telemetry.tracingEnabled` / `region` config knobs are parsed but dormant). The kernel-side work is the still-open gap in the kernel ROADMAP §"Telemetry: OTLP Metrics Export and Distributed Tracing", targeted ~Sprint 0.12 / v0.12 of the consolidated 1.0 GA roadmap. ADR-031 therefore remains blocked on that milestone, not on 0.8.0; the earlier "0.8.0" pin was a drift in this note (corrected 2026-06-08). ADR-030 was previously listed as reserved; it was promoted to ACCEPTED 2026-05-17 in the same downstream review that closed Phase 4C — see the linked file. ADR-033 was similarly listed as reserved earlier on 2026-05-18 pending the RFC; it was promoted to ACCEPTED the same day after the RFC was accepted — see `exeris-kernel/docs/rfc/RFC-2026-05-18-kernel-diagnostics-spi.md`. ADR-034 covers the symmetric client-side body codec SPI (parallel to `HttpResponseBodyEncoder` from ADR-009/0.5.0) plus the tier-neutral `KernelWebClient` facade in `exeris-kernel-core`. ADR-034 supersedes ADR-026 (the original `CommunityWebClient` placement) because that decision leaked the Community tier into the build-time surface consumed by `exeris-tooling/KernelClientGenerator`; the supersession lands in lockstep across `exeris-kernel` (SPI + facade), `exeris-tooling` (generator FQN update), and `exeris-kernel-enterprise` (link stub). Pre-1.0 has no external SPI consumers, so the supersession is in-scope for v0.8 Sprint 6. ADR-036 (reserved 2026-06-03) closes the fourth codec quadrant left open after ADR-034: server-side request-body **decode** (`HttpRequestBodyDecoder` + registry + `HttpRequestDecodingContext`), mirroring the client-side `HttpResponseBodyDecoder`. Its motivation is not symmetry-for-symmetry but removal of a build-time Wall breach — today `exeris-tooling/KernelHandlerGenerator` emits a hard-wired Jackson `MAPPER` + `JacksonException` catch into every generated controller's `parseBody`, the only codec quadrant with no SPI seam. Lands lockstep across `exeris-kernel` (SPI + `CommunityJsonRequestBodyDecoder` driver + TCK), `exeris-tooling` (generator resolves the registry instead of inlining), and `exeris-kernel-enterprise` (link stub); in-scope for v0.8 Sprint 7. ADR-038 (accepted 2026-06-05) is the **SDK-side realization** of the ecosystem capability-composition model fixed in ADR-024: it records where the `@CapabilityModule`/`@Provides`/`@Requires`/`@CapabilityLifecycle` annotations are defined (`exeris-sdk-annotations`, new `capability` subpackage), that services are referenced by `Class<?>` and persisted in the AST as source-written name strings (tooling-normalized to FQN), and that the lifecycle interface, licensing taxonomy (ADR-023), and `cap-manifest.json` discovery format stay out of the SDK. It is to ADR-024 what ADR-037 is to the `-io` module: a thin "implements" record owned by `exeris-sdk`, driven by [RFC-2026-06-03](../exeris-sdk/docs/rfc/RFC-2026-06-03-capability-annotation-surface.md). ADR-041 (accepted 2026-06-09) is owned by `exeris-spring-runtime` and follows the single-decider accepted-on-merge pattern: it records the decision to re-activate Spring Boot's servlet-web-gated OAuth2 resource-server `JwtDecoder` inside Compatibility Mode when the app runs Exeris-hosted with `spring.main.web-application-type=none`, so a brownfield JWT resource server keeps working after migrating onto the Exeris runtime. It is **narrow by design** — it covers only the resource-server JWT decoder (built with public Spring Security factories, mirroring Spring Boot's own validator wiring) and explicitly defers the broader "which servlet-web-gated auto-configs does Exeris compat re-activate under `none`" policy to a future RFC. Content and the thin decoder implementation land together in the `exeris-spring-runtime` PR that introduces `docs/adr/ADR-041-compat-resource-server-security-under-none.md`. ADR-045 (accepted 2026-06-25) resolves the client-side retry deferral that ADR-026 (§"no implicit retry") and ADR-032 (Alternative D) both named but left open: an opt-in `HttpRetryPolicy` SPI in `eu.exeris.kernel.spi.http` (`HttpRetryPolicy` / `HttpAttemptOutcome` / `RetryDecision`) consumed by `KernelWebClient` through one constructor parameter (default `none()`, preserving the ADR-026 surface), with the **behavioural** policy — idempotency gate, `502/503/504` + transport retry, `Retry-After`/`SHED_LOAD` (ADR-010) handling, capped exponential backoff + jitter — shipped in `CommunityHttpRetryPolicy`, **not** baked into the SPI, so consumer alignment (tooling / migration tool / BudgetHQ) can tune defaults without ADR churn. The retry seam re-encodes the typed body per attempt (no `LoanedBuffer` retained across attempts), so the codec path's zero-leak invariant is untouched. Content drafted on the kernel v0.10 train (Sprint 4 — HTTP Client codec/retry); lands lockstep across `exeris-kernel` (SPI + `KernelWebClient` loop + `CommunityHttpRetryPolicy` + TCK) and `exeris-tooling` / `exeris-kernel-enterprise` (link stubs). ADR-046 (reserved 2026-06-29) carries the body-codec matrix (ADR-009/034/036) onto the **events** surface: a tier-neutral `EventPayloadCodec` + `EventPayloadCodecRegistry` in `eu.exeris.kernel.spi.events.codec`, registry-selected by `(payloadType, contentType)` with a Community JSON driver, resolved **in the generated `*EventPublisher`** (ADR-036 "site B") so `EventBus`/`EventEngine` stay byte-for-byte unchanged and the wiring rides a new optional `KernelProviders.EVENT_PAYLOAD_CODEC_REGISTRY` slot (the `EVENT_STREAM_READER`/`EVENT_STREAM_APPENDER` precedent, **not** `HttpKernelProviders`). It unblocks the EV1 runtime half — today the generated publisher ships `EventPayload.empty()`, so generated domain events carry no data. Strictly additive (SPI types + driver + slot + `AbstractEventPayloadCodecTck`); lands lockstep with the `exeris-tooling` `KernelEventGenerator` rewrite that builds the redacted payload object and resolves the registry. Content is DRAFT in `exeris-kernel`; **targeted v0.10**, sequenced with the Events `topic` item. The kernel SPI + Community driver + TCK land first (the slot ships defined-but-unbound, the `EVENT_STREAM_*` precedent); the bootstrap slot-binding + generator rewrite follow lockstep. ADR-047 (accepted 2026-06-29) is a per-repo `exeris-tooling` decision driven by [RFC-2026-06-28](../exeris-tooling/docs/rfc/RFC-2026-06-28-presentation-view-emitter-tooling.md): wire the `@View` leaf field facet end-to-end — the processor populates `ComponentNodeMetadata.field` from field-level `@UI` render hints and the codegen-ts `ViewGenerator` emits the control from it — making `@View` the carrier of field-level render detail and the functional prerequisite to deprecating `@UI` (the SDK already shipped the `UIFieldMetadata`-on-leaf half; the `@View` first slice left the facet `null`). Front-only facet, so no Java emitter counterpart (Java∪TS parity by construction); carries the recurring ADR-042 `-io` reader-mirror obligation for the leaf `UIFieldMetadata`. Accepted 2026-06-29 with the founder's decisions baked in (unify on `@View`; the leaf facet is `ViewFieldMetadata`, the renamed/expanded successor of `UIFieldMetadata`; entity-driven expansion is the model; no dual-path; `@UI` removed); content in `exeris-tooling/docs/adr/ADR-047-view-leaf-field-facet-and-ui-subsumption.md`. ADR-048 (reserved 2026-06-29) holds the number for the cross-app contract mesh — generated contract registry + remote client + saga remote-dispatch (T12; capability twin T17), target 0.7.0. It is ADR-triggering and RFC-gated: codegen-core today carries only the closed-world `CapabilityGraph`, so the cross-app surface is a net-new pipeline shape designed via an RFC before content lands. ADR-055 (accepted 2026-07-30) closes the last unimplemented predicate of ADR-024's four-predicate validator: predicate 4, the cap-tier Wall. It is ADR-triggering on two counts. First, **input shape** — the tooling pipeline has until now consumed exactly one input, the processor-emitted `DomainMetadata`/`capability_*.json` corpus (a hard constraint of ADR-015); a Wall guard must read the *compiled* cap classes, which is a second input class and therefore a pipeline-shape change, not a refactor. Second, **enforcement locus** — ADR-024 obligation 3 placed the Wall in per-repo ArchUnit-style guards and conceded that "modifying or disabling the guards is a registry violation reported through periodic audits", i.e. a social control; moving the check into the pipeline that already runs `@Requires` resolution (which the ADR-024 body itself anticipates at §"The Wall, extended to capabilities") makes it mechanical and non-disableable, and obligation 3's per-repo guards become defence-in-depth rather than the primary gate. Two implementation findings are load-bearing enough to record with the decision: (a) **bytecode, not sources** — founder-ruled 2026-07-29, because a source scan sees only the cap's own files and is blind to a forbidden type that arrives transitively when a dependency changes; (b) the scan needs **no new dependency and specifically not ASM** — `exeris-tooling` enforces JDK exactly `[26,27)`, so the JDK-standard Class-File API (JEP 484, final in JDK 24) suffices, and a constant-pool `ClassEntry` walk alone is *unsound* (it misses types appearing only in field/method descriptors or generic `Signature` attributes, so a `void configure(ApplicationContext)` cap method would pass), making the extraction surface — pool ∪ descriptors ∪ signatures ∪ annotations — the load-bearing part of the design rather than the forbidden-prefix list. Note also that the ADR-024 body's clause "an SKU manifest that contains a cap with disabled or stale Wall guards is rejected by the kernel at boot" is **stale** post the 2026-06-17 "Validation Stamp Lifecycle" amendment, which made the kernel cap-blind; ADR-055 records the correct locus (build-time tooling gate, boot-time stamp assertion in the platform composition runtime) without reopening that amendment. ADR-058 (accepted 2026-07-31) is ADR-triggering on the same **pipeline-shape** ground as ADR-055, one layer out: until now every emitted artefact landed in a single output tree owned by one `OutputWriter` and one T13 manifest, and a generated *test* cannot — emitted under `src/main/generated` it would compile into the application artefact and put JUnit on its runtime classpath. The second root is therefore a shape change, not a new generator. Its second half is a contract this repo cannot enforce and must therefore state: tooling emits no `pom.xml`, so whatever a generated test imports becomes a hard requirement on every downstream project that sets `-Dexeris.tests=true`. The ruling fixes that at JUnit 5 + AssertJ and emits the doubles instead of mocking (an `HttpExchange` recorder overriding even the interface's default `respond(...)` overloads, which would otherwise need a runtime-bound encoder; a service stub that subclasses the generated service). That makes three properties of generated code load-bearing rather than incidental — `public`, non-final, assignment-only constructors — and the e2e gate enforces them by **running** the emitted tests through the JUnit Platform launcher rather than merely compiling them, since a test emitter whose output is never executed is exactly the inert-output failure mode the repo rejects elsewhere. The FE half of T2 is deliberately a separate slice: the emitted Angular app declares `"test": "ng test"` but ships no runner and no test dependencies, so emitting specs without wiring `@angular/build:unit-test` (Vitest, founder-ruled the same day) would produce specs nobody can run.

## Cross-repo stubs

| #   | Authoritative repo       | Stubs in                                                                                                                                                                                                                                                                                                                                  |
|-----|--------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 001 | exeris-docs              | [exeris-kernel/docs/adr/ADR-001.link.md](../exeris-kernel/docs/adr/ADR-001.link.md), `exeris-kernel-enterprise/docs/adr/ADR-001.link.md` *(private repo)*                                                                                                                                            |
| 006 | exeris-docs              | [exeris-kernel/docs/adr/ADR-006.link.md](../exeris-kernel/docs/adr/ADR-006.link.md)                                                                                                                                                                                                                                                       |
| 018 | exeris-kernel-enterprise | [exeris-telemetry-spec/docs/adr/ADR-018.link.md](../exeris-telemetry-spec/docs/adr/ADR-018.link.md), `exeris-enterprise-observability/docs/adr/ADR-018.link.md` *(private repo)*                                                                                                              |
| 020 | exeris-docs              | `exeris-kernel-enterprise/docs/adr/ADR-020.link.md` *(private repo)*                                                                                                                                                                                                                                 |
| 024 | exeris-docs              | [exeris-platform/docs/adr/ADR-024.link.md](../exeris-platform/docs/adr/ADR-024.link.md) (platform = deploy-time control plane per the 2026-06-25 amendment; stub updated 2026-07-21 for the boot-conductor call-site amendment)                                                                                                             |
| 025 | exeris-ai-bridge         | [exeris-kernel/docs/adr/ADR-025.link.md](../exeris-kernel/docs/adr/ADR-025.link.md), [exeris-platform/docs/adr/ADR-025.link.md](../exeris-platform/docs/adr/ADR-025.link.md)                                                                                                                                                              |
| 030 | exeris-spring-runtime    | _(stub pending — `exeris-kernel/docs/adr/ADR-030.link.md` to be added; ADR-030 documents the Spring-side seam binding `KernelProviders.GRAPH_ENGINE`, so kernel-side awareness is appropriate even though no kernel SPI change ships with the ADR)_                                                                                       |
| 033 | exeris-kernel            | [exeris-ai-bridge/docs/adr/ADR-033.link.md](../exeris-ai-bridge/docs/adr/ADR-033.link.md), `exeris-kernel-enterprise/docs/adr/ADR-033.link.md` *(private repo)*                                                                                                                                       |
| 034 | exeris-kernel            | [exeris-tooling/docs/adr/ADR-034.link.md](../exeris-tooling/docs/adr/ADR-034.link.md), `exeris-kernel-enterprise/docs/adr/ADR-034.link.md` *(private repo)*                                                                                                                                          |
| 036 | exeris-kernel            | [exeris-tooling/docs/adr/ADR-036.link.md](../exeris-tooling/docs/adr/ADR-036.link.md) (TOOL-139 generator rewrite — PR #67), `exeris-kernel-enterprise/docs/adr/ADR-036.link.md` *(private repo; PR #30)*                                                                                            |
| 037 | exeris-sdk               | [exeris-platform/docs/adr/ADR-037.link.md](../exeris-platform/docs/adr/ADR-037.link.md) (LSP consumes `exeris-sdk-source-model-io`), [exeris-tooling/docs/adr/ADR-037.link.md](../exeris-tooling/docs/adr/ADR-037.link.md) (codegen must not inherit JavaParser)                                                                          |
| 038 | exeris-sdk               | [exeris-tooling/docs/adr/ADR-038.link.md](../exeris-tooling/docs/adr/ADR-038.link.md) (processor reads the capability annotations; codegen consumes the AST records), [exeris-platform/docs/adr/ADR-038.link.md](../exeris-platform/docs/adr/ADR-038.link.md) (LSP `exeris/listCapabilities` consumes the AST records)                      |
| 042 | exeris-sdk               | [exeris-tooling/docs/adr/ADR-042.link.md](../exeris-tooling/docs/adr/ADR-042.link.md) (codegen emits `sourceDigest` + `schemaVersion` baseline-trust fields into `exeris-metadata/<entity>.json`), [exeris-platform/docs/adr/ADR-042.link.md](../exeris-platform/docs/adr/ADR-042.link.md) (LSP `exeris/applyMutation` consumes `MutationOp`/`MutationResult` + AST-level conflict detection) |
| 046 | exeris-kernel            | [exeris-tooling/docs/adr/ADR-046.link.md](../exeris-tooling/docs/adr/ADR-046.link.md) (the `KernelEventGenerator` `*EventPublisher` EV1 codec-resolution rewrite — redacted `<Event>Payload` record + `KernelProviders.eventPayloadCodecRegistry()` resolution + producer-side JFR — PR #123) |
| 054 | exeris-sdk               | [exeris-tooling/docs/adr/ADR-054.link.md](../exeris-tooling/docs/adr/ADR-054.link.md) (the tooling lockstep: SDK pin → 0.9.0, dead `ValidationMetadataSchema` TS mirror removed, constraint reads sourced from `@Validation`; filed retrospectively — PR #153) |
| 059 | exeris-sdk               | [exeris-tooling/docs/adr/ADR-059.link.md](../exeris-tooling/docs/adr/ADR-059.link.md) (obligation 5 — `dataScope` extraction, `tenantScoped` fallback-with-warning, `UNIVERSE` → kernel `sharedScopeKey`; the stub's call-site table is the migration checklist — every raw `tenantScoped()` read must move to `effectiveDataScope()` in the same slice, since a partial migration emits a table whose RLS policy, tenant column and index set disagree — PR #153), [exeris-platform/docs/adr/ADR-059.link.md](../exeris-platform/docs/adr/ADR-059.link.md) (awareness — LSP reads `DomainMetadata`; canonical read is `effectiveDataScope()`, and the `SchemaVersion` → `"0.10.0"` move degrades `exeris/applyMutation` to `NO_BASELINE` until codegen re-stamps — PR #17) |
| 063 | exeris-spring-runtime    | _(stub pending — `exeris-kernel/docs/adr/ADR-063.link.md` to be added; ADR-063 binds `HttpKernelProviders.HTTP_ROUTE_POLICY` and is the consumer kernel ADR-061 §5 gates on, so kernel-side awareness is appropriate even though no kernel SPI change ships with it — same shape as ADR-030)_ |
