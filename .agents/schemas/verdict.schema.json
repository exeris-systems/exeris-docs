{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Review verdict (exeris-docs)",
  "description": "The bundle's shape, narrowed to this repository's reviewing roles and scope vocabulary. The three-state `decision` and the `checks_run` contract are the base's; only the vocabulary is local.",
  "allOf": [
    { "$ref": "../vendor/exeris-agents-1.1.0/schemas/verdict.base.schema.json" },
    {
      "properties": {
        "agent": {
          "enum": [
            "exeris-docs-architect",
            "exeris-docs-adr-registry-keeper",
            "exeris-docs-evaluator"
          ]
        },
        "scope_class": {
          "enum": ["docs-only", "record", "standard", "agent-layer", "registry", "mixed"]
        }
      }
    }
  ]
}
